# 小说到动漫短剧的 Agent 全链路设计

> 本文档是持续维护入口。当前系统的唯一创作输入可以只是一本小说；系统负责完成小说理解、动漫改编、剧本化、生产设计、镜头生成、审片返修和粗剪导出。

## 1. 系统边界

用户最少只需要提供：

- 一本小说；
- 基本版权或使用授权确认。

系统不能假设用户已经提供视频提示词、分镜、角色图或拍摄说明。这些内容必须由后续节点逐层产生，并保留来源、版本和人工修改记录。

### 原著保真原则：改编不等于静默删除

系统必须把“原著事实是否保留”和“这一段投入多少动画作画”分开。高质量角色设定在所有镜头中保持一致，但动画密度可以按剧情价值分配。每个原著剧情单元都进入 `StoryCoverageLedger`，记录原文位置、叙事作用、人物弧光、伏笔/回收关系、保留等级、计划集数和表现方式。允许完整场景、短场景、对白、旁白、蒙太奇、视觉插入或延后到后续集；任何彻底删除只能作为 `OMIT_PROPOSAL` 提交并由人工确认，Agent 无权静默删除。

项目同时保存 `FaithfulnessProfile` 和 `NarrativeLossBudget`。当用户选择高保真改编时，系统优先增加集数、使用低动画密度但重新设计的二维镜头、旁白或蒙太奇来保存内容，而不是为了维持短时长自动砍掉人物关系、世界观、伏笔和情绪铺垫。

### 审美北极星：像动画团队制作，而不是像 AI 生成

系统的目标不是给视频套上“日本动漫风格”，而是复现二维动画的生产逻辑：角色设定、布局、关键姿势、停帧、有限动画、爆发作画、背景分层、赛璐璐阴影、二维摄影和后期合成。每个镜头都必须看得出明确的导演选择，不能依赖模型默认的流畅运动、写实光效和通用“电影感”。

首版目标媒介固定为 `HAND_DRAWN_2D_ANIME`。`DYNAMIC_COMIC`（漫画格推拉）、`LIVE2D_CUTOUT`（纸片骨骼/木偶动画）、`CEL_SHADED_3D`（三维模型套二维材质）和 `PARALLAX_ONLY`（仅靠景深分层移动）不作为质量降级方案。真正的有限动画允许停帧和少动，但停帧之前必须有为动画重新设计的 Layout、关键姿势和时机；不能直接拿完成漫画格做缩放、摇移和局部摆动来冒充二维动画。

以下现象定义为“AI 味”并进入质量门禁：线条和五官漂移、皮肤或材质过度光滑、伪 3D 景深、无目的漂浮运镜、所有元素同时运动、动作没有关键姿势、表演只有通用表情、角色与背景不在同一绘制体系、纹理或光影逐帧闪烁。系统只能通过固定资产、镜头设计、受控生成、审片返修和人工美术把这些问题压低，不能承诺单靠提示词完全消除。

完整主链：

```text
小说原文
→ 小说结构化与索引
→ 小说事实 Canon
→ 动漫改编方案
→ 分集大纲
→ 分场剧本
→ 动漫生产规格
→ 导演解读与分镜
→ 美术设定与资产锁定
→ Layout
→ Animatic 与时长锁定
→ 关键帧原画
→ 原画检查
→ 中间画、清稿与上色
→ 背景绘制
→ 二维摄影与镜头合成
→ 正式配音与声音同步
→ 镜头审片、按工序返修与下一镜
→ 剪辑、后期合成与导出
→ 数据回流与策略更新
```

## 2. 完整节点

### 第一阶段：小说接收与理解

#### 00 项目创建与小说上传

接收小说文件，记录来源、版本、语言、版权状态和用户明确要求。这个节点不负责理解剧情。

#### 01 小说源文件标准化

确定性服务完成文本提取、OCR、章节识别、段落编号、去重和不可变原文快照，输出 `NovelSource` 和 `SourceIndex`。

#### 02 Novel Analyst Agent

文本模型按“段落 → 章节 → 全书”分层阅读，提取人物、关系、时间线、地点、道具、世界规则、事件、伏笔和未决冲突。每条结论必须引用原文位置，不能直接改编剧情。

#### 03 Novel Canon 合并与冲突检查

确定性合并器与校验器把各章节分析结果合成 `NovelCanon`，检查同名人物、时间矛盾、身份变化和来源缺失。

#### 04 Story Bible 确认

将确认后的小说事实晋级为版本化 `StoryBible`。高影响冲突或模型无法判断的内容进入人工确认。

### 第二阶段：动漫改编与剧本化

#### 05 Production Medium Profile

首版将生产媒介锁定为 `HAND_DRAWN_2D_ANIME`，不向下兼容漫剧、纸片动画或三维赛璐璐。配置必须包含角色设定规范、线条、配色、赛璐璐阴影、背景绘制、图层关系、Layout、关键姿势、原画/中间动作计划、停帧、一拍二、局部一拍一、爆发作画、二维摄影、声音和禁止写实漂移规则。同时生成版本化 `AnimeAuthenticityProfile`，作为资产、镜头、Prompt、评价和返修的共同验收依据。

#### 06 Adaptation Writer Agent：改编规划任务

读取小说 Canon、`FaithfulnessProfile`、媒介配置和目标时长，先为每个剧情单元建立 `StoryCoverageLedger`，再提出完整场景、压缩场景、合并、重排、对白、旁白、蒙太奇、视觉插入、延后或删除提案，输出 `AdaptationPlan`。原著事实与改编决定必须分开记录，删除只能是带理由和影响分析的提案。

#### 07 改编方案门禁

检查改编是否破坏核心人物、主线、关系变化、世界规则、关键伏笔及其回收、受众限制和集数预算；检查所有 `MUST_RETAIN` 单元是否已经分配表现方式。超出 `NarrativeLossBudget`、存在无去向剧情或提出删除关键内容时必须人工确认。

#### 08 分集与分场规划

将改编方案拆成集、场和剧情节拍，明确每集目标、开场钩子、冲突升级、高潮和结尾悬念，输出 `EpisodeOutline` 和 `SceneCard`。

#### 09 Adaptation Writer Agent：剧本写作任务

把当前集大纲写成可生产的分场剧本，包括场景、出场人物、动作、对白、旁白、声音线索和场景入口/出口状态。此处不写最终视频提示词。

#### 10 Script QA 与剧本锁定

检查剧情完整性、Canon 一致性、人物动机、时间线、对白长度、动漫可表现性和单集时长，输出版本化 `EpisodeScript`。

### 第三阶段：前期制作（Pre-production）

#### 11 Director Planner Agent：导演解读与分镜

读取分场剧本、Story Bible 和二维动画生产规格，完成场景节拍、Director's Read、表演意图和连续分镜。每个分镜必须标注景别、构图、画面方向、人物走位、剪辑点、预计时长和声音意图，输出 `Storyboard`，但此时不生成最终视频。

#### 12 Art Director / Asset Designer Agent：美术设定

建立版本化 `ArtBible`，设计动画生产需要的角色比例表、转面图、表情表、口型表、手势表、服装拆分、道具设定、色彩脚本、场景设定和背景风格。它们是后续 Layout、原画和背景绘制的统一依据，不是拿来直接推拉的完成漫画格。

#### 13 美术资产生成与设定锁定

调用图片或语音模型生成候选设定资产，经过身份、线条、配色、结构和可动画性检查后，晋级为不可覆盖的 `AssetVersion`。正式角色设定和美术设定锁定后，后续镜头不能自行重新设计。

#### 14 Layout

根据分镜和锁定资产，为每个镜头制作正式 Layout：确定角色尺寸、透视、站位、视线、构图、背景范围、前景遮挡、画面方向和二维摄影空间。Layout 是原画、背景和摄影共同遵守的镜头蓝图。

#### 15 Animatic、时长与临时声音锁定

将分镜和 Layout 组成动态分镜，加入临时对白、旁白和音效，验证单镜头时长、表演停顿、剪辑节奏和整集长度。通过后锁定 `Animatic`、`TimingSheet`、`DialogueTiming` 和 `AnimationEffortPlan`；二维原画不能在节奏尚未确定时直接开工。

`AnimationEffortPlan` 将镜头分为：A 级高潮/爆发作画、B 级标准角色表演、C 级有限动画、D 级叙事蒙太奇或插入镜头。所有等级都必须重新设计 Layout、构图、关键姿势和时间，D 级也不能直接拿漫画格推拉。角色设计质量不随等级降低，降低的只是中间画数量和运动复杂度。

### 第四阶段：二维动画中期制作（Production）

#### 16 Animation Director Agent：动作与原画设计

读取 Layout、Animatic、角色设定和当前镜头的表演意图，规划关键姿势、动作弧线、停帧、预备动作、跟随动作、一拍二/局部一拍一以及需要爆发作画的位置，输出 `KeyAnimationPlan`。它不能修改分镜剧情和角色设计。

#### 17 关键帧原画

依据 `KeyAnimationPlan` 生成或绘制关键姿势原画。角色结构、轮廓、表情、重心和动作方向必须在关键帧阶段先成立；不能让视频模型自由补完整个动作以后再尝试修正。

#### 18 原画检查与修正

Quality Evaluator 以动画监督任务检查角色是否走形、姿势是否有力、重心是否合理、动作是否可读、关键帧之间是否能连接。原画不合格时只返修原画，不进入中间画。

#### 19 中间画、清稿与上色

在通过的关键帧之间生成必要中间画，并完成线条清理、颜色填充、阴影层和口型/眨眼等局部动画。中间画数量由 TimingSheet 决定；不能把“越流畅”当作越高质量。

#### 20 背景绘制

依据同一 Layout 独立制作背景画、前景层和需要移动的背景层，保持透视、色彩脚本和光照方向一致。角色背景不能由视频模型在运动过程中逐帧重新发明。

#### 21 二维摄影与镜头合成

把上色角色、背景、前景、特效和摄影动作分层合成。允许平移、推近、背景卷动、多平面视差和必要特效，但必须服从分镜；禁止用无目的镜头漂移掩盖原画不足。

#### 22 正式配音与声音同步

在 Animatic 和镜头时长锁定后完成正式配音、旁白、音效和音乐，并校准口型、停顿、动作落点和声音透视。必要时可以在前期先录制临时对白，但最终声音必须与确认画面精确同步。

#### 23 镜头审片与 Canon 对账

Quality Evaluator 同时读取 Storyboard、Layout、TimingSheet、`AnimeAuthenticityProfile` 和合成镜头，检查剧情、角色结构、二维动画质感、动作时机、背景、摄影、声音与连续性。出现 `MOTION_COMIC_DRIFT`、`CUTOUT_PUPPET_DRIFT`、`PARALLAX_ONLY`、`CEL_SHADED_3D_DRIFT`、`PANEL_PAN_ZOOM`、`LINE_BOIL`、`FACE_MELT`、`OVER_ANIMATION`、`CAMERA_FLOAT` 或 `TEXTURE_FLICKER` 时不得晋级。

#### 24 Repair Advisor Agent：按工序返修

根据证据判断错误来自分镜、Layout、原画、中间画、上色、背景、摄影还是声音，并只把任务退回真正出错的工序。禁止所有问题都通过重新生成完整视频解决。

#### 25 连续性检查与下一镜

只有通过的合成镜头才能成为 `AcceptedTake`。系统记录真实出口姿势、位置、表情、服装、道具、画面方向、背景状态和声音状态，再开始下一镜的 Layout 与动画生产。

### 第五阶段：后期制作（Post-production）

#### 26 序列质检与剪辑

按 Animatic 和实际镜头结果完成整场、整集剪辑，检查节奏、镜头衔接、剧情信息、动作连续性和声音连续性。

#### 27 后期合成

完成最终特效、调色统一、字幕、对白、音效、背景音乐、响度和片头片尾等工作，输出可审看的成片母版。

#### 28 Delivery、数据回流与回归

完成格式、分辨率、帧率、字幕、声音、命名和平台要求检查后导出；记录各工序返修率、可用秒成本、人工修改和最终采用情况。Agent、模型、Prompt 或评价器升级前必须运行固定回归集。

### 贯穿生产阶段的 AI 执行底座

`Generation Router`、`Prompt Compiler Agent`、生成前门禁和 `Executor` 不再作为动画制作工序出现。它们是底层执行能力，在美术设定、Layout、原画、中间画、背景、摄影和声音节点需要调用模型时提供：

- 当前工序对应的模型与生成模式；
- 最小上下文和锁定资产；
- Prompt、参考素材职责和参数；
- Schema、版本、授权、预算与安全校验；
- 异步执行、候选记录、成本、重试和审计。

任何模型调用都必须服务于一个明确的二维动画工序，不能绕过工序直接从剧本生成最终镜头。

## 3. 专职 Agent

首版需要八类专职 Agent：

| Agent | 主要职责 | 明确禁止 |
|---|---|---|
| Novel Analyst | 理解长篇小说并建立有来源的事实 | 改编剧情、设计镜头 |
| Adaptation Writer | 制定改编方案、分集分场并编写剧本 | 暗改原著事实、生成视频 |
| Director Planner | 导演解读、表演设计、分镜和剪辑意图 | 修改 Canon、兼任动画监督、直接生成媒体 |
| Art Director / Asset Designer | 美术设定及角色、场景、道具、服装和声音资产规格 | 覆盖正式设定、用完成漫画格代替生产资产 |
| Animation Director | Layout 约束、关键姿势、动作时机、原画和中间画计划 | 修改剧本和分镜、让模型自由生成整段动作 |
| Prompt Compiler | 将当前二维工序任务编译为对应模型请求 | 改变剧情、跨过工序直接请求最终镜头 |
| Quality Evaluator | 分工序独立评价候选并提供证据和失败码 | 修改作品、作最终业务决定 |
| Repair Advisor | 识别出错工序并提出单变量最小修复方案 | 无限重试、把所有问题退回完整视频重生 |

工作流、上下文组装、Canon 合并、连续性验证、模型路由、参数校验、预算、执行器和版本控制必须是确定性服务，不交给 Agent 自由决定。

## 4. 核心数据链

```text
NovelSource
→ SourceIndex
→ NovelCanon
→ StoryBible
→ FaithfulnessProfile / NarrativeLossBudget
→ ProductionMediumProfile
→ AdaptationPlan
→ StoryCoverageLedger
→ EpisodeOutline
→ EpisodeScript
→ DirectorRead / Storyboard
→ ArtBible
→ AssetVersion
→ Layout
→ Animatic / TimingSheet / DialogueTiming / AnimationEffortPlan
→ KeyAnimationPlan
→ KeyAnimation
→ Inbetween / CleanCel / ColoredCel
→ BackgroundPlate
→ CompositeShot
→ TakeReview
→ AcceptedTake / ObservedState
→ RoughCut
```

Agent 之间不传聊天记录，只传这些带 ID、版本、来源和状态的数据对象。`PromptSpec` 和 `GenerationRun` 仍然记录每次模型调用，但属于贯穿各工序的技术审计链，不代替上面的二维动画生产产物。

## 5. 长篇小说的上下文原则

1. 任何 Agent 都不默认读取整本小说。
2. 原文切分后保留稳定的章节、段落和句子 ID，任何事实都能回到原文。
3. Novel Analyst 使用分层处理：局部抽取、章节汇总、全书合并。
4. 后续 Agent 主要读取 Story Bible、当前集、当前场和必要原文证据。
5. Director Planner 可以读取全剧故事主线和相邻剧情摘要；执行模型只读取当前 Layout、原画、背景或声音等工序所需的最小任务包。
6. 被拒绝的候选不能进入 Canon，也不能成为下一镜参考。
7. 用户修改、原著事实、模型推断和改编决定必须分别标记。
8. 所有模型调用固定输入版本，不能只读取“最新版”。

## 6. Seedance Skill OS 的使用位置

- `Director's Read`、`Directing Engine`、`2D Anime Grammar`：用于 Production Medium Profile、Director Planner 和 Animation Director。
- Character、Camera、Motion、Lighting、Audio、Style、VFX、语言与 Anti-slop：由 Prompt Compiler 按美术设定、Layout、原画、背景、摄影或声音工序的实际需要加载。
- Capability、Allocation、Reference Contract、首尾帧和 Surface Profile：用于 Generation Router 与 Prompt Compiler。
- Sequence State、Continuation、Continuity：用于接受结果后的状态更新和下一镜。
- Retake、Troubleshoot、Failure Atlas：用于 Quality Evaluator 和 Repair Advisor。
- Copyright、Filter、API Status、专业制作与 Delivery QC：用于安全、执行和交付门禁。

这些技能是规则和知识，不对应同数量的物理 Agent。

## 7. GitHub 开源参考与采用边界

截至 2026-08-20，尚未发现一个成熟仓库能够独立完成“长篇小说 → 忠实改编 → 日式手绘二维动画成片”的全链路。采用策略是：借鉴 Agent 的工作流和数据契约，再把二维动画专项模型作为可替换工具接入具体工序，而不是把某个一键视频仓库当作系统底座。

| 仓库 | 可借鉴位置 | 采用边界 |
|---|---|---|
| [seedance-2.0](https://github.com/Emily2040/seedance-2.0) | 导演解读、2D Anime Grammar、镜头编译、连续性、重拍与质检规则 | 作为 Skill 知识库，不代替正式二维动画工序 |
| [OpenToonz](https://github.com/opentoonz/opentoonz) | Xsheet/Timing Sheet、分层、描线上色、摄影合成等传统二维生产概念；Modified BSD | 首先借鉴数据模型和工作台结构，不要求首版直接嵌入桌面软件 |
| [LayerInbetween](https://github.com/MarkMoHR/LayerInbetween) | 原画之间的线稿补间、遮挡处理和自动分层；GPL-3.0 | 新项目且成熟度有限，先做实验性 Provider，商用集成需处理 GPL 边界 |
| [AniDoc](https://github.com/ant-research/AniDoc) | 用角色设计参考约束整段线稿上色；Apache-2.0 | 只负责上色/一致性，不负责生成合格原画和动作设计 |
| [BasicPBC](https://github.com/ykdai/BasicPBC) | 赛璐璐线稿区域匹配与批量上色 | 代码许可仅限非商业用途，不能直接作为商业工坊默认实现 |
| [ToonCrafter](https://github.com/Doubiiu/ToonCrafter) | 两张卡通关键画之间的短序列生成、草图约束；Apache-2.0 | 只作为特定镜头补间候选，不能代替 Animation Director 和原画计划 |
| [Index-AniSora](https://github.com/bilibili/Index-anisora) | 动漫视频生成、首尾帧/多帧控制、动漫专项评价与奖励模型 | 作为镜头生成或评测 Provider；仓库代码许可与具体模型权重条款必须分别审计 |
| [AgentCine](https://github.com/pengchengneo/AgentCine) / [PenShot](https://github.com/neopen/story-shot-agent) / [OpenMontage](https://github.com/calesthio/OpenMontage) | Agent 图执行、人工审批点、异步任务、重试、记忆、追溯和模型路由 | 仅借鉴平台架构；它们本身没有补齐 Layout、原画、中间画、上色和二维摄影链 |

明确不采用动态漫画、Ken Burns 推拉、纸片骨骼或“每镜一张图直接图生视频”作为主生产路线。这类仓库最多借鉴任务管理、素材版本和时间线 UI。

## 8. 维护规则

1. 新增或修改节点时，必须记录输入、必需上下文、可选上下文、禁止上下文、模型或工具、输出和失败去向。
2. 调整数据传递时，必须同步更新 Context Policy 和相关 Schema。
3. 节点只提交提案；Story Bible、正式资产和 Accepted Take 的晋级由工作流与门禁决定。
4. 已接受视频观察到的真实状态优先于计划状态。
5. 不再使用 AI 生成图片表达精确架构；全链路以本文件的结构化文字为准。
6. 后续按节点顺序逐个补充详细设计，不在一次讨论中混合多个节点。

## 9. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-20 | 调研 GitHub 开源生态，确认采用“Agent 工作流参考 + 传统二维数据契约 + 分工序专项模型 Provider”的组合路线，并记录许可证和采用边界。 |
| 2026-08-20 | 增加原著保真机制：`StoryCoverageLedger`、`FaithfulnessProfile`、`NarrativeLossBudget` 和分级 `AnimationEffortPlan`，禁止改编 Agent 静默删除剧情。 |
| 2026-08-20 | 采用正式二维动画生产线：前期分镜/美术/Layout/Animatic，中期原画/中间画/上色/背景/摄影合成/配音，后期剪辑与交付；新增 Animation Director Agent。 |
| 2026-08-19 | 将首版媒介锁定为真正的手绘二维动画，明确排除漫画格推拉、纸片骨骼动画、仅视差运动和三维赛璐璐降级路线。 |
| 2026-08-19 | 将“像动画团队制作、避免浓重 AI 味”设为审美北极星，新增 `AnimeAuthenticityProfile` 和专门失败码。 |
| 2026-08-19 | 将系统起点从剧本改为一本小说，增加小说理解、动漫改编和剧本化链路；专职 Agent 调整为七类。 |
| 2026-08-19 | 停止使用生成图片作为精确架构依据，改用结构化文字维护。 |
| 2026-08-19 | 建立初版 Agent 全链路设计。 |
