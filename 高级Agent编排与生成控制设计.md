# 高级 Agent 编排与图片/视频生成控制设计

> 版本：2026-08-19  
> 适用阶段：基础功能已经完成，开始建设稳定性、连续性、自动返修和规模化生产能力。  
> 目标：把“导演经验”和“模型经验”编码为可回放、可评测、可升级的生产系统。

## 1. 这里所说的“保证”

图片和视频生成是概率过程，系统无法保证每一次模型调用都得到完美画面。工程上应保证的是：

1. 缺少必要资产或状态时，任务不会进入生成。
2. 所有模型参数在提交前通过任务类型和参数约束校验。
3. 每次调用都有固定版本、输入快照、成本、结果和评价记录。
4. 未通过质量门禁的结果不会成为正式资产或进入成片。
5. 自动重试不会无限循环，也不会突破镜头预算。
6. 失败后能够定位到具体维度，并选择编辑、重生成、拆镜或转人工。
7. Agent、模型或提示词升级前必须跑固定回归集，低于基线则禁止发布。
8. 工作流进程中断后能够从已完成节点继续，而不是从头生成。

因此，质量保证对象是 **生产流程和验收标准**，不是单次模型随机结果。

## 2. 核心架构结论

不要构建一个能够自由修改所有内容的“超级导演 Agent”。推荐采用：

```text
确定性工作流引擎
  -> 版本化状态与预算
  -> 专业 Agent 提出计划/诊断/补丁
  -> 规则校验器批准或拒绝
  -> 模型执行器调用图片/视频模型
  -> 质量评估器输出结构化报告
  -> 工作流决定通过、返修、降级或人工审核
```

### 为什么这样设计

- **[官方依据]** 火山方舟 Managed Agents 官方文档指出，需要细粒度控制 Agent Loop 时应选模型 API；托管 Agent 更适合长时间、多轮工具调用和有状态异步任务。
- **[官方依据]** Multi Agent 使用协调器和独立子线程，官方要求明确各子 Agent 的专长、输入、输出和协作顺序；当前只支持一层委派，协调器最多配置 20 个子 Agent，且会锁定子 Agent 版本。
- **[工程依据]** Temporal Workflow 要求工作流逻辑保持确定性，外部 API、数据库、LLM 和文件操作放入 Activity；事件历史用于重放并恢复中断前状态。

### 推荐职责边界

| 组件 | 是否使用 LLM/VLM | 职责 |
|---|---|---|
| Workflow Orchestrator | 否 | 状态机、依赖、并发、预算、重试、超时、人工节点 |
| Director Planner | 是 | 将剧情节拍编译为镜头图，不直接调用生成模型 |
| Continuity Guardian | 规则 + 模型 | 维护人物、服装、道具、空间和时间连续性 |
| Asset Designer | 是 | 生成角色/场景/道具的结构化图片规格 |
| Prompt Compiler | 模板 + 模型 | 把结构化规格编译为某个模型的提示词和素材绑定 |
| Generation Router | 规则优先 | 根据任务类型、复杂度、成本和历史数据选择模型/模式 |
| Image/Video Executor | 否 | 调用供应商 API，处理异步任务、回调、限流和幂等 |
| Quality Evaluator | CV/VLM + 规则 | 分维度评分并给出证据，不直接修改作品 |
| Repair Planner | 规则 + 模型 | 根据失败码生成一个最小修改方案 |
| Human Review | 人 | 处理审美、剧情歧义、授权和达到重试上限的任务 |

关键原则：**Agent 不能直接修改事实库。Agent 只能提交 Patch Proposal，由确定性校验器提交。**

### 首版物理 Agent 数量

上表是逻辑职责，不应一项对应一个独立 Agent。首版建议只部署 5 个版本化 Agent 配置：

1. `director-planner`：剧情节拍、镜头规划和表演意图。
2. `asset-designer`：角色、服装、场景、道具和关键帧规格。
3. `prompt-compiler`：图片/视频模型专用提示词和素材职责绑定。
4. `quality-evaluator`：按固定 rubric 输出证据化评价，不做修改。
5. `repair-advisor`：读取失败码和评价证据，提出最小修改 Patch。

Continuity、Router、Executor、Validator 和 Workflow 必须优先实现为确定性服务。只有规则无法表达的语义判断才调用模型。

## 3. 生产状态模型

### 3.1 四层状态

```text
Story Bible       跨集稳定事实
Episode Snapshot  本集开始时的人物/道具/关系状态
Scene State       场景内空间、服装、光线、情绪和道具状态
Shot Spec         单镜头入口、动作、出口和生成计划
```

每层都必须有 `version`。镜头生成时记录实际读取的版本，禁止只保存“当前最新值”。

### 3.2 事实与表现分离

```json
{
  "fact": {
    "character_id": "char_lin_xia",
    "wardrobe_id": "wardrobe_gray_suit_v3",
    "prop_ids": ["prop_old_photo_v2"],
    "location_id": "scene_meeting_room_v4"
  },
  "performance": {
    "emotion_start": "controlled",
    "emotion_end": "alert",
    "action": "places_folder_then_looks_at_photo",
    "camera": "medium_to_close_slow_push"
  }
}
```

- `fact` 是连续性硬约束，Agent 不得自行改写。
- `performance` 可以产生多个导演候选。
- 提示词只是编译结果，不是真实数据源。

### 3.3 Patch 提交协议

```json
{
  "base_version": 12,
  "agent_version": "continuity-guardian@3.4.1",
  "reason": "角色在上一镜已摘下胸针",
  "operations": [
    {
      "op": "replace",
      "path": "/shots/E01_S014/fact/wardrobe_id",
      "value": "wardrobe_gray_suit_no_brooch_v1"
    }
  ],
  "evidence": ["E01_S013.exit_state.wardrobe"]
}
```

校验器需要检查：版本是否冲突、目标字段是否允许修改、证据是否存在、是否影响已锁定镜头。

## 4. Agent 编排状态机

### 4.1 主流程

```text
SCRIPT_LOCKED
  -> STORY_STATE_READY
  -> SHOT_GRAPH_PLANNED
  -> ASSET_REQUIREMENTS_READY
  -> ASSETS_PROMOTED
  -> SHOTS_READY
  -> GENERATING
  -> SHOT_QA
       -> PASSED
       -> REPAIRABLE -> REPAIRING -> SHOT_QA
       -> SPLIT_REQUIRED -> REPLAN_SHOT
       -> HUMAN_REQUIRED
  -> SEQUENCE_QA
  -> ROUGH_CUT_READY
```

每个转换都必须有明确 `preconditions` 和 `postconditions`，不能仅依赖 Agent 自述“已经完成”。

### 4.2 Agent 输入输出契约

所有 Agent 输出必须满足 JSON Schema。自由文本只允许放在 `rationale`，不得承载关键控制字段。

```json
{
  "task_id": "task_E01_S014_plan",
  "input_versions": {
    "story_bible": 8,
    "episode_snapshot": 3,
    "scene_state": 5
  },
  "result": {},
  "warnings": [],
  "blocking_errors": [],
  "confidence": 0.86,
  "agent": {
    "name": "director-planner",
    "version": "2.3.0",
    "model": "pinned-model-id"
  }
}
```

阻断规则：

- JSON Schema 不通过：不重试生成任务，先重试 Agent 输出。
- `blocking_errors` 非空：不进入下游。
- `confidence` 低于经过校准的阈值：进入规则降级或人工审核。
- 输入版本已变化：输出作废，重新规划。

### 4.3 并行边界

可以并行：

- 不同角色的初始资产。
- 不同独立场景的场景图。
- 已锁定资产后的非连续镜头。
- 同一镜头的受控候选生成。
- 画面检查、音频检查、合规检查等独立评价维度。

必须串行：

- 同一人物的新服装状态依赖上一状态时。
- 镜头 B 的入口状态依赖镜头 A 的生成出口时。
- 视频延长、轨道补全和连续长镜头。
- 资产编辑依赖上一资产版本时。
- 同一状态字段的两个 Patch Proposal。

使用 `character_id + wardrobe_id`、`scene_id`、`shot_sequence_id` 作为细粒度锁，不锁整个项目。

### 4.4 幂等和断点续跑

每次外部调用使用：

```text
idempotency_key = project_id + node_id + input_hash + policy_version
```

相同输入、相同策略版本不得重复扣费创建任务。LLM、图片、视频、数据库和文件操作全部作为 Temporal Activity；Workflow 只保存确定性决策和 Activity 结果引用。

## 5. 图片生成控制

### 5.1 资产不是图片文件，而是版本化资产包

### 角色资产包

```text
identity_anchor       独立清晰面部参考
neutral_full_body     中性站姿全身参考
wardrobe_variants     每套剧情服装独立图片
expression_refs       需要时单独保存，不拼接成大图
pose_refs             只提供姿态职责
style_anchor          项目统一画风参考
negative_history      已确认失败的参考组合和原因
```

**重要区别：** 可以在资产库保存多个角度，但向 Seedance 2.0 提交人物参考时，不要把同一人物的多视图拼成一张图。官方提示词指南指出，多视图可能被识别成多个主体并加剧身份漂移。推荐提交独立大头照 + 独立全身照。

### 场景资产包

```text
master_wide           空间主视图
reverse_angle         反向机位参考
key_zones             门、窗、桌、楼梯等空间锚点
lighting_states       日/夜/火灾/停电等状态
layout_constraints    人物可站位区域、轴线和出入口
```

### 道具资产包

必须记录尺寸感、材质、颜色、持有人、出现状态和破损状态。关键剧情道具需要独立主体图，不能只从复杂场景中裁切。

### 5.2 图片模型路由

根据火山官方当前能力，可按任务而不是统一模型路由：

| 任务 | 推荐能力路线 | 官方能力依据 |
|---|---|---|
| 精确局部编辑、图层拆分、透明背景素材 | Seedream 5.0 Pro | 支持交互编辑、最多 16 个图层拆分和透明通道 |
| 角色/分镜连续组图 | Seedream 5.0 Lite 或 4.5 | 支持组图和关联图片序列 |
| 多参考融合、换装、风格迁移 | Seedream 5.0 Lite/4.5/4.0 | 官方提示词指南支持多图替换、组合和迁移 |
| 首尾关键帧精确构建 | Seedream 图片编辑/多图融合 | 官方“Seedream 助力 Seedance”最佳实践 |

路由器先输出能力需求，再匹配模型。不要让 Agent 仅凭模型名称选择。

```json
{
  "capabilities": ["identity_preservation", "wardrobe_transfer", "single_image"],
  "hard_requirements": {
    "aspect_ratio": "9:16",
    "transparent_background": false,
    "max_reference_images": 3
  },
  "quality_tier": "hero_asset",
  "budget_tier": "high"
}
```

### 5.3 图片提示词编译

不要把一整套 Story Bible 塞入提示词。只编译当前任务需要的字段：

```text
任务类型 + 图片用途
主体及不可变特征
当前服装/道具
动作或姿态
场景与空间关系
构图、景别、视角
光线、色彩、风格
需要修改的内容
必须保持不变的内容
输出约束
```

示例：

```text
生成 9:16 影视视频首帧静帧画面。
参考图片1的人物面部、发型和年龄感；让人物穿图片2的灰色职业套装。
人物站在图片3会议室长桌左侧，右手按住蓝色文件夹，身体朝向桌子，视线看向画面右侧。
中景，机位略低于视线，人物位于画面左三分之一，为右侧对手保留空间。
冷白室内顶光，写实电影感。
保持会议室布局、人物面部和服装款式不变；不增加其他人物、文字、Logo 和水印。
```

**[官方依据]** Seedream 指南建议使用自然语言明确“主体 + 行为 + 环境”，编辑任务说明对象、修改内容和保持不变的部分；多图任务需明确每张图的职责。

### 5.4 图片资产晋级流程

```text
DRAFT -> CANDIDATE -> VERIFIED -> PROMOTED -> LOCKED
```

- `DRAFT`：低成本探索，不允许进入视频生产。
- `CANDIDATE`：自动检查通过，可供人工/Agent 比较。
- `VERIFIED`：通过身份、服装、风格、构图和结构检查。
- `PROMOTED`：被指定为某个剧情状态的正式资产。
- `LOCKED`：已有下游镜头依赖；修改需新建版本，不能覆盖。

### 图片硬门禁

- 输出尺寸和宽高比正确。
- 主体数量正确。
- 人物身份相似度达到经人工标注校准的阈值。
- 服装主色、关键配饰和道具匹配。
- 没有非预期文字、Logo、水印或拼接残留。
- 图像适合作为视频关键帧：没有极端裁切，动作起始姿态可继续运动。

### 图片评分不要只用一个总分

```json
{
  "identity": 0.91,
  "wardrobe": 0.96,
  "style": 0.88,
  "composition": 0.84,
  "anatomy": 0.93,
  "prompt_adherence": 0.89,
  "unexpected_text": false,
  "hard_gate_passed": true
}
```

身份错误不能被高构图分抵消。任何硬门禁失败都不得晋级。

## 6. 视频生成控制

### 6.1 先选任务模式，再写提示词

### 视频模式路由表

| 条件 | 首选模式 | 原因 |
|---|---|---|
| 无固定角色、环境或抽象过场 | 文生视频 | 参考约束少 |
| 起始构图必须准确，出口开放 | 首帧生视频 | 锁定入口画面 |
| 起点和终点都必须准确 | 首尾帧生视频 | 控制动作结果、变装、运镜落点和转场 |
| 需要复用人物、风格、动作、运镜或音色 | 全模态参考 | 不同素材承担不同职责 |
| 基础视频大部分正确，只需局部增删改 | 视频编辑 | 避免整镜重抽 |
| 需要沿用已有动作和画面继续发展 | 视频延长 | 保持既有视频入口状态 |
| 多段视频之间缺过渡 | 轨道补全/桥接镜头 | 为剪辑提供稳定衔接 |

### Seedance 2.5 参数预校验

**[官方]** Seedance 2.5 会结合输入素材和提示词判断任务类型；类型与参数不一致会触发同步或异步错误。

```text
视频编辑：
  reference_video >= 1
  reference_video.duration in [4, 30]
  ratio = adaptive
  duration = -1
  prompt 明确包含编辑/增加/删除/修改/替换意图

视频延长：
  reference_video >= 1
  ratio = adaptive
  duration in [4, 30] or -1
  prompt 明确包含向前/向后延长、延续或续写意图

首帧/首尾帧：
  first_frame = 1
  last_frame = 0 or 1
  ratio = adaptive
  输出宽高比跟随首帧
```

必须在任务进入队列前完成校验，不能等模型异步失败后才发现。

### 6.2 可生成性复杂度评分

在调用视频模型前给每个镜头计算复杂度：

```text
complexity =
  2 * 同镜头主体数
  + 2 * 人物交互数
  + 动作阶段数
  + 运镜变化数
  + 场景状态变化数
  + 变装/变形次数 * 2
  + 台词角色数
  + 特效层数
```

初始策略建议通过回归数据校准：

- `0-5`：直接生成。
- `6-8`：优先首尾帧或增加动作参考。
- `9-11`：拆成两个生成片段。
- `>=12`：必须重新设计镜头，不允许自动批量提交。

这些阈值是工程初始值，不是官方限制。上线后按题材、模型版本和实际一次可用率分别训练。

### 6.3 视频提示词必须同时描述空间层和时间层

```text
素材职责
入口状态
按时间分段的动作
镜头运动
出口状态
声音/台词
风格与画质
禁止项
```

```text
@图片1只参考林夏的面部和发型，@图片2参考灰色职业套装，@图片3参考会议室布局。
保持角色身份、服装、会议室空间和冷白光线一致。

[0-2 秒] 中景固定镜头。林夏站在桌子左侧，把蓝色文件夹平放到桌面，右手留在文件夹上。
[2-4 秒] 镜头缓慢推进。画面右侧一只手将旧照片推入画面，林夏先低头看照片。
[4-6 秒] 推进到近景。林夏抬眼看向画面右侧，表情由克制变为警惕，身体位置不变。

出口状态：照片停在文件夹上方，林夏右手仍按住文件夹，视线朝右。
无额外人物，无字幕、文字、Logo、水印；手指结构正常；不改变服装和场景布局。
```

### 提示词编译检查

- 每份素材只承担明确职责。
- 时间段总和与目标时长一致。
- 动作具有可见的开始和结束状态。
- 不同时要求固定镜头和大幅环绕运镜。
- 不同时要求人物保持静止和完成大范围位移。
- 台词长度能在分配时间内自然说完。
- 出口状态能够成为下一镜入口。

### 6.4 关键帧控制

**[官方]** Seedream 助力 Seedance 最佳实践明确建议使用图片模型精准构建首帧和尾帧；首尾帧适用于主体一致性、变装、运镜落点、商品状态和强风格转换。

系统应自动选择首尾帧的典型条件：

- 镜头必须落在指定姿态或指定构图。
- 角色需要换装、转身、进出画面或跨场景。
- 商品必须从关闭变为打开等明确状态。
- 镜头要推进到某个建筑、人物或道具特写。
- 风格化画面必须在整个运动中保持统一。

关键帧生成时提示词加入“影视视频静帧画面”，并保证图片宽高比与目标视频一致。官方文档明确提醒宽高比差异会导致裁切或变形。

### 6.5 生成时长策略

**[官方]** Seedance 2.5 单次支持 4-30 秒，并加强了长叙事能力。  
**[工程策略]** 支持 30 秒不等于所有镜头都应该生成 30 秒。

- 情绪特写、反应镜头：优先短镜头。
- 单一连续表演或不可切断运镜：可尝试长生成。
- 包含多个叙事动作、多个角色轮流说话或多次场景变化：默认拆镜。
- 长镜头只有在回归数据证明一次可用率和可剪辑性更优时才能成为默认策略。

## 7. 质量评价与自动返修

### 7.1 三层评价

### 第一层：确定性检查

- 分辨率、比例、时长、编码、文件完整性。
- 黑帧、冻结帧、重复帧、闪烁峰值。
- 音轨存在、采样率、响度、削波。
- 水印、异常文字和安全审核状态。

### 第二层：单镜头语义检查

- 人物身份和数量。
- 服装、场景、道具一致性。
- 动作是否完成，方向是否正确。
- 表情变化是否符合剧情。
- 景别、运镜和焦点是否符合 Shot Spec。
- 台词角色、发音、音色和口型。

### 第三层：跨镜头连续性检查

- 人物进出方向和视线匹配。
- 服装、伤势、手持道具和空间位置。
- 光线、天气和时间状态。
- 动作衔接及稳定切点。
- 音色、环境声和音乐连续性。

### 7.2 评价器隔离

- 生成 Agent 不能给自己的结果最终放行。
- 质量评价读取 Shot Spec 和产物，但不读取生成 Agent 的“自我解释”。
- VLM 评分必须输出时间码或检测框作为证据。
- 自动评分阈值必须用人工标注集校准，不能凭经验设置后永久不变。
- 每批成片随机抽取一定比例人工复核，监控评价器漂移。

### 7.3 失败码到修复动作

| 失败码 | 首选修复 | 不应做的事 |
|---|---|---|
| `IDENTITY_DRIFT` | 换独立身份锚点、减少人物参考、重要素材前置 | 只增加更多形容词 |
| `STYLE_DRIFT` | 先把关键帧转成目标风格，使用首尾帧/风格参考 | 混用写实和动漫参考图 |
| `SUBJECT_COUNT_ERROR` | 减少同镜头人物或先合成分组画面 | 继续增加角色参考图 |
| `ACTION_INCOMPLETE` | 缩短动作链、延长合理时长或拆镜 | 原提示词不变连续抽卡 |
| `MOTION_CONTRADICTION` | 删除冲突运镜/动作约束 | 同时要求固定与环绕 |
| `END_STATE_MISMATCH` | 构建尾帧，切换首尾帧模式 | 只在负向词中描述终点 |
| `LOCAL_OBJECT_ERROR` | 使用视频编辑局部修复 | 整镜重生成所有正确内容 |
| `EXTENSION_JUMP` | 明确入口状态，生成桥接/轨道补全 | 无限继续延长劣化片段 |
| `TEXT_OR_WATERMARK` | 清理输入文字，使用字幕擦除或局部编辑 | 带文字素材直接继续参考 |
| `SPEECH_MISMATCH` | 缩短台词、拆说话镜头、重新绑定音色 | 在同镜头增加更多说话人 |

### 7.4 最小修改重试

每次重试只允许改变一类主要变量：

```json
{
  "attempt": 3,
  "primary_failure": "END_STATE_MISMATCH",
  "change_set": {
    "generation_mode": "first_last_frame",
    "last_frame_asset": "asset_E01_S014_end_v2"
  },
  "unchanged": [
    "character_assets",
    "scene_asset",
    "duration",
    "audio",
    "style"
  ]
}
```

否则无法知道修复为何有效，也无法沉淀路由策略。

### 7.5 停止条件

自动循环必须在以下任一条件停止：

- 达到镜头等级的最大尝试次数。
- 达到镜头预算。
- 连续两次评分提升低于最小增益。
- 同一失败码连续两次且修复策略无新分支。
- 需要修改锁定剧情事实或正式资产。
- 涉及审美选择、授权或内容含义变化。

停止后只允许：人工选择已有候选、批准拆镜、提高预算或修改创作意图。

## 8. 模型路由和学习闭环

### 8.1 路由不是固定 if/else，也不能完全交给 Agent

首版采用“硬约束过滤 + 数据评分排序”：

```text
候选模型
  -> 过滤不支持任务类型/时长/比例/素材数量的模型
  -> 过滤合规和预算不满足的模型
  -> 根据历史一次可用率、成本、耗时和题材匹配度评分
  -> Agent 只在前两名之间解释特殊创作需求
```

```text
route_score =
  0.40 * predicted_pass_rate
  + 0.20 * continuity_score
  + 0.15 * latency_score
  + 0.15 * cost_score
  + 0.10 * editability_score
```

权重必须按业务目标配置，并通过离线回放验证。

### 8.2 决策上下文

每次路由至少使用：

- 镜头类型、价值等级和复杂度。
- 人物数量、动作数、台词长度和参考素材数。
- 是否要求精确首帧/尾帧。
- 历史同类镜头在模型版本上的通过率。
- 剩余预算、队列等待时间和交付期限。
- 后期是否需要 MOV、透明素材、图层或可编辑性。

### 8.3 数据记录

```json
{
  "attempt_id": "att_01J...",
  "shot_id": "E01_S014",
  "shot_spec_version": 7,
  "router_policy_version": "router@1.8.0",
  "model_id": "pinned-model-version",
  "task_type": "first_last_frame",
  "prompt_hash": "sha256:...",
  "asset_versions": ["char@12", "wardrobe@3", "scene@4"],
  "parameters": {},
  "latency_ms": 0,
  "cost": 0,
  "evaluation": {},
  "selected": false,
  "failure_code": null
}
```

没有这份数据，就无法真正优化高产率，只能凭主观印象换模型。

## 9. 回归测试与发布门禁

### 9.1 建立黄金回归集

每个首发题材至少覆盖：

- 单人近景情绪变化。
- 双人正反打。
- 人物走位和拿取道具。
- 变装或状态变化。
- 多人场景。
- 快速动作。
- 首尾帧运镜。
- 视频编辑和延长。
- 有声对白。
- 2D/3D/写实目标画风。

每条用例保存锁定输入、预期硬约束、人工评分和允许的误差范围。

### 9.2 任何升级都要跑回归

升级对象包括：

- Agent 系统提示词。
- Agent 模型或版本。
- Prompt Compiler 模板。
- Generation Router 策略。
- 图片/视频模型版本。
- 质量评价器、阈值和权重。
- 资产预处理和视频后处理代码。

发布门禁示例：

```text
硬门禁通过率不得下降
角色身份通过率下降 <= 1 个百分点
一次可用率不得下降超过 2 个百分点
可用秒成本不得上升超过 5%，除非质量显著提升并获批准
P95 工作流耗时不得上升超过 10%
人工复核与自动评价一致率必须达到校准目标
```

具体数值应由当前生产基线确定，上述仅为初始模板。

### 9.3 Shadow 和 Canary

1. 新策略先对历史任务离线回放。
2. 再以 Shadow 模式生成评价，不影响正式成片。
3. 只放量到少量 C 级镜头。
4. 达标后扩大到 B 级镜头。
5. A 级钩子、高潮和集尾镜头最后开放。

## 10. 实施优先级

### P0：先做，直接决定是否稳定

1. Canonical Shot Spec 和状态版本。
2. Agent JSON Schema 和 Patch Proposal。
3. 任务类型/参数预校验器。
4. Generation Attempt 全链路日志。
5. 失败码分类和有限重试状态机。
6. 图片资产晋级和锁定机制。
7. 单镜头硬门禁。
8. 预算、幂等和断点续跑。

### P1：提高一次可用率

1. 复杂度评分与自动拆镜。
2. 图片模型和视频模式路由。
3. 首尾关键帧自动构建。
4. 身份、服装、道具和动作的自动评价。
5. Repair Planner 最小修改策略。
6. 跨镜头连续性检查。

### P2：形成长期优势

1. 题材级路由统计模型。
2. 自动评价器与人工评分校准。
3. 黄金回归集和升级门禁。
4. 发布数据回流到剧情和镜头策略。
5. 按团队、题材和模型版本的成功策略库。

## 11. 参考文档

### Agent 与工作流

1. [方舟 Managed Agents 概述](https://docs.volcengine.com/docs/82379/2553713?lang=zh)，最近更新 2026-08-17。Agent、Environment、Session、Event、状态持久化、断点续跑和模型 API 的适用边界。
2. [方舟 Multi Agent](https://docs.volcengine.com/docs/82379/2553730?lang=zh)，最近更新 2026-07-10。协调器、子线程、委派流程、版本锁定、一层嵌套和观测机制。
3. [Temporal Workflow](https://docs.temporal.io/workflows)，工作流确定性、Event History、重放、恢复和 Activity 边界。

### 图片生成

1. [图片生成 API](https://docs.volcengine.com/docs/82379/1541523?lang=zh)，最近更新 2026-08-12。Seedream 5.0 Pro/Lite、4.5、4.0 的单图、组图、多图、图层和参数能力。
2. [Seedream 4.0-5.0 提示词指南](https://docs.volcengine.com/docs/82379/1829186?lang=zh)，最近更新 2026-05-11。自然语言提示、精确编辑、多图职责、组图和参考生成。
3. [Seedream 4.0 助力 Seedance 生视频最佳实践](https://docs.volcengine.com/docs/82379/1951250?lang=zh)，最近更新 2026-08-13。首帧、首尾帧、关键帧、风格和宽高比控制。

### 视频生成

1. [Doubao Seedance 2.5 教程](https://docs.volcengine.com/docs/82379/2607688?lang=zh)，最近更新 2026-08-17。任务类型、30 秒生成、50 个素材、时间戳编辑、参数限制和 MOV 输出。
2. [Doubao Seedance 2.0 系列提示词指南](https://docs.volcengine.com/docs/82379/2222480?lang=zh)，最近更新 2026-08-17。空间层/时间层、素材职责、编辑、延长及常见失败处理。
3. [Doubao Seedance 2.0 系列教程](https://docs.volcengine.com/docs/82379/2291680?lang=zh)。API、样片模式、连续视频、Webhook 和使用限制。
4. [火山剧创短剧创作](https://docs.volcengine.com/docs/87744/2261814?lang=zh)，最近更新 2026-08-10。平台实际的资产、分镜、生成、编辑、续写、字幕擦除、超分和导出流程。

## 12. 学习顺序

不再按产品页面学习，按系统问题学习：

1. 先实现 Canonical Shot Spec，理解事实、表现和提示词为什么必须分离。
2. 再实现 Agent 契约、版本和 Patch Proposal。
3. 用 20 个镜头建立第一版复杂度、模式路由和失败码。
4. 建角色/场景资产包和图片晋级门禁。
5. 接入首帧、首尾帧、参考、编辑、延长五条视频路径。
6. 建单镜头评价、最小修改返修和停止条件。
7. 最后做跨镜头连续性、统计路由和回归发布门禁。

完成前六步后，系统才具备“可控生产”；完成第七步后，才开始具备“越生产越稳定”的能力。
