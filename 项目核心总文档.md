# 小说到手绘二维动漫工业化平台核心总文档

> 唯一阅读入口。日常设计、开发和评审只看本文件。  
> 其他 `.md` 文件仅作为官方资料副本和研究依据，不要求阅读。  
> 更新时间：2026-08-20

## 1. 产品目标

建设一套以一本小说为最小输入、输出可发布手绘二维动漫成片的工业化生产系统。用户不需要先准备视频提示词、分镜、角色图或导演说明；系统负责小说理解、忠实改编、剧本化、前期设计、二维动画制作、声音和后期交付。

首版目标媒介固定为 `HAND_DRAWN_2D_ANIME`。`DYNAMIC_COMIC`（漫画格推拉）、`LIVE2D_CUTOUT`（纸片骨骼/木偶动画）、`CEL_SHADED_3D`（三维赛璐璐）和 `PARALLAX_ONLY`（仅靠景深分层移动）不作为质量降级方案。

核心竞争力不是拥有更多模型或更多 Agent，而是：

1. 原著事实、人物弧光、伏笔和关系变化可追溯，不被 Agent 静默删除。
2. 角色、美术、Layout、原画、背景、摄影和声音遵守同一套二维生产设定。
3. 角色、服装、场景、道具和声音跨镜头一致。
4. 每个模型只服务于明确工序，避免从小说直接跳到最终视频。
5. 失败能够定位到分镜、Layout、原画、中间画、上色、背景、摄影或声音并局部返修。
6. 所有过程可回放、可统计、可恢复、可控制成本。
7. 生产数据能够持续优化模型路由、作画分配和导演策略。

系统最终衡量的是 **可用成片分钟**，不是模型调用次数或生成视频总秒数。

## 2. 可以保证什么

生成模型是概率系统，不能保证每次调用都完美。平台必须保证以下工程属性：

- 缺少必要资产或状态时不允许生成。
- 任务类型和参数不合法时不进入模型队列。
- 每次调用都有固定版本、输入快照、成本和评价记录。
- 未通过质量门禁的结果不能成为正式资产或进入成片。
- 自动重试有次数、预算和最小收益限制。
- 失败能够归类为具体问题，并选择编辑、重生成、拆镜或人工处理。
- Agent、提示词、模型和评价器升级前必须通过固定回归集。
- 工作流中断后能够从已完成节点继续。

## 3. 总体架构

```text
小说原文与授权确认
  -> 原文标准化、稳定索引与 Novel Canon
  -> Story Bible、原著覆盖账本与改编方案
  -> 分集、分场和可生产剧本
  -> 导演解读、连续分镜和美术设定
  -> Layout、Animatic、Timing Sheet 与作画等级
  -> 关键帧原画和原画检查
  -> 中间画、清稿、上色与背景绘制
  -> 二维摄影、镜头合成与正式声音
  -> Take Review：观察真实首尾状态
  -> Canon Reconciliation：接受或按出错工序返修
  -> 从已接受结果开始下一镜
  -> 剪辑、后期合成与交付
  -> 生产和发布数据回流
```

长序列遵循：**全书建 Canon、分集改编、前期锁定、逐镜生产、观察真实结果、更新 Canon、再继续下一镜**。计划状态用于生产前决策；一旦候选被接受，真实画面和声音中观察到的状态拥有更高权威，不能继续拿原计划假装已经发生。

### 核心技术边界

| 层 | 推荐实现 | 职责 |
|---|---|---|
| 前端 | Next.js、React、TypeScript | 项目、资产、分镜、审核、预览和专业参数 |
| API | FastAPI | 业务接口、鉴权和领域服务 |
| 工作流 | Temporal | 长任务、依赖、重试、超时、恢复和人工节点 |
| 数据 | PostgreSQL、Redis | 事实状态、版本、任务、缓存和队列 |
| 文件 | TOS/S3、CDN | 图片、视频、音频和中间产物 |
| 检索 | pgvector/Qdrant | Story Bible、历史策略和资产检索 |
| 媒体 | FFmpeg | 抽帧、拼接、响度、转码和基础检测 |
| 二维生产工作台 | Web 工作台 + OpenToonz 式 Xsheet/Timeline 数据模型 | Layout、摄影表、原画、中间画、分层上色、背景和摄影合成协作 |
| 模型层 | 统一模型适配器 | 文本、图片、视频、语音、音乐、口型、视觉评价 |

所有 LLM、图片、视频、二维专项模型、数据库和文件操作均作为 Temporal Activity。Workflow 只保存确定性状态和决策。AniSora、Seedance、ToonCrafter、AniDoc 等都属于可替换 Provider，不是 Agent，也不是整条生产线。

## 4. Agent 编排

### 4.1 核心原则

不要让一个超级 Agent 自由规划、生成、评价并修改所有内容。

推荐结构：

```text
确定性 Workflow
  -> Agent 提交结构化计划或补丁
  -> Validator 校验
  -> Executor 执行模型调用
  -> 独立 Evaluator 验收
  -> Workflow 决定下一状态
```

Agent 不能直接修改 Story Bible 或正式资产，只能提交 `Patch Proposal`。

### 4.2 首版保留 8 类专职 Agent

| Agent | 输入 | 输出 | 不允许做的事 |
|---|---|---|---|
| Novel Analyst | 小说原文分片、Source Index | 带原文证据的人物、关系、时间线、地点、道具、世界规则和事件提案 | 改编剧情、设计镜头 |
| Adaptation Writer | Novel Canon、Story Bible、保真配置、媒介和时长约束 | Story Coverage Ledger、改编方案、分集分场与 Episode Script | 静默删除原著事实、生成最终媒体 |
| Director Planner | Episode Script、Story Bible、二维媒介规范 | Director's Read、表演意图、Storyboard 和剪辑意图 | 修改 Canon、兼任动画监督、调用最终生成模型 |
| Art Director / Asset Designer | Storyboard、角色和场景需求、媒介规范 | Art Bible、角色/场景/道具/服装/声音资产规格 | 覆盖正式设定、用完成漫画格代替生产资产 |
| Animation Director | Layout、Animatic、Timing Sheet、角色设定和表演意图 | Key Animation Plan、关键姿势、动作弧线、停帧与中间画计划 | 修改剧本和分镜、让模型自由生成完整动作 |
| Prompt Compiler | 当前二维工序任务、锁定资产、目标 Provider | 工序专用 Prompt、参考素材职责和参数 | 改变剧情、绕过工序直接请求最终成片 |
| Quality Evaluator | 当前工序规格、候选产物 | 分维度评分、证据、失败码和晋级建议 | 修改作品、评价自己的生成结果、作最终业务决定 |
| Repair Advisor | 失败码、评价证据、工序血缘和历史尝试 | 指向真实出错工序的单变量最小修复 Patch | 无限重试、把所有问题都退回完整视频重生 |

以下能力必须优先做成确定性服务，而不是 Agent：

- Continuity Validator
- Novel Canon Merger / Validator
- Story Coverage Validator
- Context Assembler
- Generation Router
- 参数校验器
- 预算和限流器
- 任务执行器
- 版本和冲突校验器
- 工作流状态机

### 4.3 Agent 节点契约

契约分四层，不能只约束最终 JSON：

| 层 | 必须定义 | 目的 |
|---|---|---|
| 调用契约 | `task_type`、输入版本、预算、截止时间、允许工具 | 防止 Agent 越权和无限探索 |
| 数据契约 | 输入/输出 JSON Schema、枚举、单位、可空规则 | 防止自然语言歧义进入执行层 |
| 写权限契约 | 可创建字段、可提议字段、禁止修改字段 | 保证 Story Bible 和正式资产不被暗改 |
| 结果契约 | 状态、证据、置信度、失败码、建议动作 | 让 Workflow 能确定性决定下一步 |

所有 Agent 使用统一信封。自由文本只能用于解释，不能承载关键控制字段：

```json
{
  "contract_version": "agent-task.v1",
  "task_id": "task_E01_S014_plan",
  "idempotency_key": "project:episode:shot:input_hash:policy_v3",
  "task_type": "PLAN_SHOT",
  "input_refs": {
    "story_bible": {"id": "sb_01", "version": 8},
    "episode_snapshot": {"id": "ep_01", "version": 3},
    "scene_state": {"id": "sc_07", "version": 5}
  },
  "constraints": {
    "deadline_ms": 30000,
    "max_tool_calls": 6,
    "max_cost_units": 2.0,
    "allowed_patch_paths": ["/shots/shot_014/performance", "/shots/shot_014/generation_plan"]
  },
  "result": {
    "status": "SUCCEEDED",
    "artifact_type": "shot-plan.v2",
    "artifact": {},
    "evidence": [],
    "warnings": [],
    "failure_codes": [],
    "confidence": 0.86,
    "recommended_next_action": "VALIDATE"
  },
  "agent": {
    "name": "director-planner",
    "version": "2.3.0",
    "model": "pinned-model-id"
  }
}
```

`result.status` 只允许：

- `SUCCEEDED`：产物完整，可进入确定性校验。
- `RETRYABLE`：输入没问题，但工具或模型调用可重试。
- `BLOCKED`：缺少资产、事实冲突或前置状态未完成。
- `HUMAN_REQUIRED`：需要审美、授权、剧情或预算决策。
- `CANCELLED`：任务已被新版本或用户操作取代。

核心产物也要独立版本化：

| 产物 | 最少字段 |
|---|---|
| `novel-source.v1` | 原始文件哈希、授权状态、语言、章节/段落/句子稳定 ID、原文位置 |
| `novel-canon.v1` | 人物、关系、时间线、地点、道具、世界规则、事件、伏笔及每条事实的原文证据 |
| `story-coverage-ledger.v1` | 原著剧情单元、叙事作用、保留等级、表现方式、计划集数、删除提案和审批状态 |
| `episode-script.v1` | 场景、人物、动作、对白、旁白、声音线索、入口/出口状态和来源追溯 |
| `art-bible.v1` | 角色比例/转面/表情/口型/服装、场景/道具、线条、配色、背景和二维摄影规范 |
| `layout.v1` | 构图、透视、角色尺寸/站位/视线、轴线、背景范围、前景遮挡和摄影空间 |
| `timing-sheet.v1` | 帧号、曝光、关键姿势、停帧、一拍二/局部一拍一、口型、摄影和声音落点 |
| `key-animation-plan.v1` | 动作弧线、预备/主体/跟随动作、关键姿势、原画编号、中间画需求和作画等级 |
| `shot-plan.v2` | 剧情目的、入口/出口状态、动作节拍、表演曲线、机位、剪切意图 |
| `asset-spec.v1` | 主体、身份锚点、服装/状态、视角、背景、用途、验收标准 |
| `generation-request.v2` | 任务类型、模型能力需求、素材职责、Prompt、参数、预算、幂等键 |
| `evaluation-report.v2` | 硬门禁、分项分数、时间码/区域证据、失败码、是否可修复 |
| `repair-plan.v1` | 根因、只改一个主变量的 Patch、预期收益、回滚点、剩余尝试数 |

阻断条件：JSON Schema 不通过；输入版本已经变化；`failure_codes` 含阻断项；置信度低于校准阈值且无确定性降级；Patch 超出允许路径；产物缺少证据或验收条件。

### 4.4 协调器决策模型

协调器不是另一个“总导演 Agent”，而是受规则约束的路由器。每个节点都执行同一闭环：

```text
读取固定版本输入
  -> 检查前置条件、预算和租约
  -> 调用一个专职 Agent 或确定性服务
  -> Schema + 权限 + 版本校验
  -> 追加事件，不原地覆盖历史
  -> 状态机决定提交、返修、降级或转人工
```

协调器只能从白名单动作中选择：`RUN_NODE`、`WAIT_DEPENDENCY`、`RETRY_ACTIVITY`、`APPLY_VALIDATED_PATCH`、`REPLAN_SHOT`、`REQUEST_HUMAN`、`CANCEL_STALE_TASK`。它不能临时创造新节点或跳过验收门禁。

推荐拓扑：

```text
Episode Workflow
  -> Scene Workflows（可并行）
       -> Asset 分支（fan-out / fan-in）
       -> Shot Workflows（按连续性依赖排序）
            -> Plan -> Compile -> Generate -> Evaluate -> Repair/Pass
  -> Sequence QA -> Rough Cut
```

只有输入集合冻结后才能 `fan-out`；只有依赖产物全部达到指定晋级状态才能 `fan-in`。不要让子 Agent 通过聊天记录隐式传递状态，跨节点只传版本化产物引用。

### 4.5 并发、补偿和恢复

- 对 `project/episode/scene/shot/asset` 使用不同粒度租约；同一资源同一时刻只有一个写租约。
- Patch 提交采用 `expected_version` 乐观锁；冲突时重新规划，禁止覆盖新状态。
- 外部生成任务一经创建必须先查询供应商状态，再决定是否重试，避免重复扣费。
- 每个副作用都定义补偿：预占预算可释放，候选可废弃，正式资产不可物理覆盖，只能撤销绑定并新建版本。
- Workflow 重放只消费事件历史；模型、数据库、对象存储和网络请求全部留在 Activity。
- 恢复点至少位于资产晋级、镜头计划锁定、生成任务创建、候选验收和正式入选之后。

### 4.6 Multi Agent 使用边界

火山方舟官方 Multi Agent 当前采用协调器和独立子线程，支持一层委派、版本锁定和事件观测。对本项目的启示：

- 明确每个子 Agent 的专长、输入和输出。
- 子任务上下文隔离，不共享整段无关历史。
- 协调器锁定子 Agent 版本，升级必须跑回归。
- 生产编排由 Workflow 决定，不能完全依赖协调器临场判断。

## 5. 状态与连续性

### 5.1 小说事实链与四层生产状态

镜头状态之前先建立不可跳过的小说事实链：

```text
NovelSource -> SourceIndex -> NovelCanon -> StoryBible
  -> FaithfulnessProfile / NarrativeLossBudget
  -> StoryCoverageLedger -> EpisodeScript
```

原著事实、用户修改、模型推断和改编决定必须分别标记。任何彻底删除都只能作为 `OMIT_PROPOSAL` 提交并由人工确认。

```text
Story Bible       跨集不可随意变化的事实
Episode Snapshot  本集开始时的关系、伤势、位置和道具状态
Scene State       当前场景的服装、光线、空间和情绪状态
Shot Spec         单镜头入口、动作、出口和生成计划
```

每层必须有版本号。生成任务记录实际读取的版本，不能只引用“最新版”。

### 5.2 事实与表现分离

```json
{
  "fact": {
    "character_id": "char_lin_xia",
    "wardrobe_id": "wardrobe_gray_suit_v3",
    "prop_ids": ["prop_old_photo_v2"],
    "location_id": "scene_meeting_room_v4"
  },
  "performance": {
    "emotion_start": "controlled",
    "emotion_end": "alert",
    "action": "places_folder_then_looks_at_photo",
    "camera": "medium_to_close_slow_push"
  }
}
```

- `fact` 是硬约束。
- `performance` 可以产生多个导演候选。
- Prompt 是编译结果，不能作为事实来源。

### 5.3 Shot Spec 必备字段

- `project_id`、`episode_id`、`scene_id`、`shot_id/clip_id`、`parent_clip_id`、`sequence_index`。
- 剧情目的和观众应感受到的内容。
- `directors_read_lane` 及对应的叙事 `authoring_state` 或非叙事用途记录。
- `already_happened`、`this_clip_only`、`reserved_for_later`，防止重演已完成动作或提前消费后续剧情。
- 计划入口/出口状态，以及生成后的实际入口/出口状态。
- 出镜人物及服装状态。
- 场景、道具和人物站位。
- 动作阶段、表情和台词。
- 景别、机位、运镜、时长和剪切点。
- 图片/视频/音频素材职责。
- 生成模式、模型能力需求和预算等级。
- `continuity_locks`、`allowed_changes`、禁止项和验收标准。
- `transition_in/out`、未闭合运动向量、音频相位和连续生成深度。

### 5.4 计划状态、观察状态与 Canon

状态必须明确区分：

| 状态类型 | 内容 | 权威来源 |
|---|---|---|
| Canonical State | 人物身份、服装版本、产品几何、固定道具、地点和授权资产 | Story Bible、正式资产版本 |
| Planned State | 本镜头预期入口、动作和出口 | Director Planner、Shot Spec |
| Observed State | 实际视频中的姿态、位置、动作相位、镜头相位、环境布置和声音相位 | 已完成候选的 Take Review |
| Accepted Canon | 人工或门禁接受后，允许下游继承的真实状态 | 对账事件和锁定候选 |

规则：

- 首镜或主动重锚镜头从 Canonical State 打开。
- 连续镜头从已接受父镜头的 `observed_end_state` 打开，不能从父镜头的 `planned_end_state` 打开。
- 实际结果提前完成后续节拍时，将该节拍标为已完成，并从后续 Prompt 移除。
- 实际结果未完成计划节拍但仍可接受时，记录 `accepted_deviations`，将未完成内容移交下一镜。
- `observed` 只证明画面和声音中真正发生的内容，不能反向证明角色动机、潜台词或授权事实。
- 被拒绝的候选不能更新 Canon，也不能成为下一镜父源。

### 5.5 场景、镜头血缘与重锚

每个连续镜头必须保存精确父镜头 ID。父镜头必须先于子镜头，禁止自指、循环、空字符串父 ID 和悬空父节点。

- 同一场景内允许从已接受视频或尾帧连续生成。
- 场景边界视为有意切镜，下一场从 Canonical State 和正式资产重锚。
- `extension_depth` 记录连续使用模型输出作为输入的层数。首版默认最多连续 2 层，硬上限 3 层；达到上限必须重锚。该阈值是初始生产策略，后续按真实漂移率校准。
- 已接受视频作为参考时携带静态和动态状态；尾帧只携带静态状态，Prompt 仍须声明未闭合动作、运镜和声音相位。
- 连续性豁免必须指向具体字段和具体角色/资产，`允许合理变化` 之类泛化文本不能解除硬约束。

### 5.6 核心数据契约

首版将 Seedance 2.0 Skill OS 的五类契约改造成平台领域模型，而不是原样复制文件：

| 本项目产物 | 来源原型 | 主要职责 | 必须增加的项目字段 |
|---|---|---|---|
| `project-state.v1` | Project State | 项目、场景、节拍、镜头、父子血缘和 Canon | 集数、团队、权限、剧本来源、授权状态 |
| `clip-contract.v1` | Clip Contract | 当前镜头唯一生成范围和连续性边界 | Shot Spec 版本、资产版本、预算等级、模型能力需求 |
| `prompt-spec.v1` | Prompt Spec | 结构化状态到最终自然语言 Prompt 的可审计中间产物 | provider profile、中文编译规则、模板和策略版本 |
| `take-review.v1` | Take Review | 记录真实首尾状态、完成节拍、偏差和判定 | 自动/人工证据、评价器版本、时间码和区域 |
| `generation-run.v1` | Generation Run | 单次生成调用和结果台账 | provider/model/region、参数、任务 ID、Webhook、耗时、成本、输出保存期限 |

二维动画生产还必须补充以下领域产物：

```text
ProductionMediumProfile / AnimeAuthenticityProfile
-> DirectorRead / Storyboard
-> ArtBible / AssetVersion
-> Layout
-> Animatic / TimingSheet / DialogueTiming / AnimationEffortPlan
-> KeyAnimationPlan / KeyAnimation
-> Inbetween / CleanCel / ColoredCel
-> BackgroundPlate / CompositeShot
-> TakeReview / AcceptedTake / ObservedState
```

Agent 之间不传整段聊天记录，只传这些带 ID、版本、来源和状态的产物引用。执行模型只获得当前工序需要的最小上下文。

JSON Schema 只负责单记录结构；父子顺序、循环、状态权威、Take Review 对账和跨镜头连续性必须由语义 Validator 处理，不能误以为 Schema 通过就等于项目状态合法。

## 6. 工作流状态机

项目级状态只表达阶段完成度：

```text
NOVEL_INGESTED
  -> NOVEL_CANON_READY
  -> STORY_BIBLE_CONFIRMED
  -> ADAPTATION_PLAN_REVIEW
  -> EPISODE_SCRIPT_LOCKED
  -> STORYBOARD_READY
  -> ART_BIBLE_LOCKED
  -> LAYOUT_READY
  -> ANIMATIC_AND_TIMING_LOCKED
  -> KEY_ANIMATION
  -> KEY_ANIMATION_QA
  -> INBETWEEN_CLEANUP_COLOR
  -> BACKGROUND_READY
  -> COMPOSITING_AND_SOUND
  -> SHOT_QA
       -> PASSED
       -> REPAIRABLE -> ROUTE_TO_FAILED_STAGE -> SHOT_QA
       -> SPLIT_REQUIRED -> STORYBOARD_OR_LAYOUT_REPLAN
       -> HUMAN_REQUIRED
  -> SEQUENCE_QA
  -> POST_PRODUCTION
  -> DELIVERY_READY
```

镜头内部先经过二维工序状态，再进入候选验收：

```text
STORYBOARDED
  -> LAYOUT_LOCKED
  -> TIMING_LOCKED
  -> KEY_ANIMATION_PLANNED
  -> KEY_ANIMATION_READY
  -> KEY_ANIMATION_VERIFIED
  -> INBETWEEN_READY
  -> CLEANUP_AND_COLOR_READY
  -> BACKGROUND_READY
  -> COMPOSITED
  -> EVALUATING
```

当某一工序调用图片、视频或二维专项模型时，其候选执行状态机为：

```text
DRAFT
  -> PLANNED
  -> WAITING_ASSETS -> READY
  -> SUBMITTING -> GENERATING
       -> RECONCILING -> GENERATING | EVALUATING | FAILED_FINAL
  -> EVALUATING
       -> PASSED -> SELECTED -> RECONCILED -> LOCKED
       -> REPAIRABLE -> REPAIR_PLANNED -> READY
       -> SPLIT_REQUIRED -> SUPERSEDED
       -> HUMAN_REQUIRED
  -> CANCELLED | FAILED_FINAL
```

所有转移由事件触发，禁止直接写 `status`：

```json
{
  "event_id": "evt_01J...",
  "event_type": "SHOT_EVALUATED",
  "aggregate_type": "SHOT",
  "aggregate_id": "shot_014",
  "expected_version": 12,
  "causation_id": "task_E01_S014_eval",
  "correlation_id": "episode_01_run_09",
  "payload": {
    "candidate_id": "vid_014_c03",
    "decision": "REPAIRABLE",
    "failure_codes": ["END_STATE_MISMATCH"]
  },
  "occurred_at": "2026-08-19T10:00:00+08:00"
}
```

关键转移表：

| 当前状态 | 事件 | Guard | 原子动作 | 失败去向 |
|---|---|---|---|---|
| `DRAFT` | `SHOT_PLAN_ACCEPTED` | 事实版本一致、计划 Schema 通过 | 保存 Shot Spec 并锁定输入版本 | `HUMAN_REQUIRED` |
| `WAITING_ASSETS` | `ASSET_DEPENDENCIES_READY` | 所有必需资产为 `PROMOTED/LOCKED` | 固化资产版本和素材职责 | 保持等待 |
| `READY` | `GENERATION_REQUESTED` | 预算、参数、幂等键和租约通过 | 预占预算、写 Attempt、进入提交 | 保持 `READY` |
| `SUBMITTING` | `PROVIDER_TASK_CREATED` | 供应商任务 ID 唯一 | 持久化任务 ID，提交事务性 Outbox | `RECONCILING` |
| `GENERATING` | `PROVIDER_COMPLETED` | 回调签名、任务 ID 和输出完整 | 登记 Candidate，释放运行租约 | `RECONCILING` |
| `EVALUATING` | `SHOT_EVALUATED` | 报告含证据且评价器版本允许 | 保存报告并选择 Pass/Repair/Split/Human | `HUMAN_REQUIRED` |
| `PASSED` | `CANDIDATE_SELECTED` | 候选仍绑定当前 Shot Spec | 写入选择理由，绑定待对账候选 | 保持 `PASSED` |
| `SELECTED` | `TAKE_RECONCILED` | Take Review 完整且绑定当前候选 | 写入 observed start/end、节拍偏差和新 Canon revision | `REPAIRABLE/HUMAN_REQUIRED` |
| `RECONCILED` | `SHOT_LOCKED` | 相邻镜头连续性和授权通过 | 锁定候选，允许下游将其作为父源 | `REPAIRABLE` |
| 任意非终态 | `INPUT_VERSION_SUPERSEDED` | 上游正式版本变化 | 取消未提交任务，标记产物过期 | `CANCELLED` |

`RECONCILING` 是恢复态：通过供应商查询、Outbox/Inbox 事件和幂等键确定任务究竟未提交、运行中还是已完成，不能凭超时猜测。

状态机必须额外满足：

- 每条命令最多产生一次有效状态转移，但可被重复投递。
- 事件只追加；修正通过新事件完成，不改历史事件。
- `LOCKED` 不是不可变文件，而是不可原地覆盖的业务版本。
- 上游事实变化必须使依赖候选变为 `STALE`，由影响范围分析决定局部重算。
- 人工操作也必须产生同样的命令、事件和审计记录，不能绕过 Workflow。

### 可并行

- 不同角色和独立场景的资产生成。
- 已锁定资产后的非连续镜头。
- 同一镜头的受控候选。
- 画面、声音、合规等独立评价维度。

### 必须串行

- 下一镜依赖上一镜出口状态。
- 同一人物状态连续变化。
- 视频延长、轨道补全和连续长镜头。
- 正式资产的连续编辑版本。
- 两个 Agent 修改同一个状态字段。

幂等键：

```text
project_id + node_id + input_hash + policy_version
```

相同输入和策略版本不得重复创建付费任务。

## 7. 小说到手绘二维动漫的完整生产链

### 7.1 原著保真和二维真实性

系统必须把“原著内容是否保留”和“这一段投入多少动画作画”分开。每个原著剧情单元进入 `StoryCoverageLedger`，记录原文位置、叙事作用、人物弧光、伏笔/回收、保留等级、计划集数和表现方式。允许完整场景、压缩场景、对白、旁白、蒙太奇、视觉插入或延后；彻底删除只能提交带理由和影响分析的 `OMIT_PROPOSAL`，Agent 无权静默删除。

项目同时保存 `FaithfulnessProfile` 和 `NarrativeLossBudget`。高保真模式优先增加集数，或使用作画密度较低但经过重新设计的二维镜头、旁白和蒙太奇保存内容，而不是自动砍掉人物关系、世界观、伏笔和情绪铺垫。

二维真实性由版本化 `AnimeAuthenticityProfile` 约束。目标不是在视频外观上添加“动漫风格”，而是遵循角色设定、Layout、关键姿势、停帧、有限动画、爆发作画、背景分层、赛璐璐阴影、二维摄影和声音设计的生产逻辑。线条/五官漂移、伪 3D 景深、无目的漂浮运镜、所有元素同时运动、没有关键姿势、通用表情、角色与背景绘制体系不一致、纹理和光影逐帧闪烁都属于质量失败。

### 7.2 二十九个生产节点

| 阶段 | 节点 | 主要输入 | 核心输出与门禁 |
|---|---|---|---|
| 小说接收 | 00 项目创建与小说上传 | 小说文件、授权确认、用户要求 | 项目记录、来源/版本/语言/版权状态；不在此节点理解剧情 |
| 小说接收 | 01 小说源文件标准化 | 原始文件 | `NovelSource`、`SourceIndex`；确定性完成提取/OCR、章节识别、段落编号、去重和不可变快照 |
| 小说理解 | 02 Novel Analyst | 原文分片和索引 | 按段落→章节→全书提取事实，每条结论必须引用原文位置 |
| 小说理解 | 03 Novel Canon 合并 | 分层分析结果 | `NovelCanon`；检查同名人物、时间矛盾、身份变化和证据缺失 |
| 小说理解 | 04 Story Bible 确认 | Novel Canon、冲突清单 | 版本化 `StoryBible`；高影响冲突转人工 |
| 改编 | 05 Production Medium Profile | 产品目标、二维媒介约束 | `HAND_DRAWN_2D_ANIME`、`AnimeAuthenticityProfile`；拒绝动态漫画、纸片骨骼、仅视差和三维赛璐璐降级 |
| 改编 | 06 Adaptation Writer：改编规划 | Canon、保真配置、媒介和时长 | `AdaptationPlan`、`StoryCoverageLedger`；原著事实与改编决定分开记录 |
| 改编 | 07 改编方案门禁 | 改编方案、覆盖账本 | 检查核心人物、主线、关系、世界规则、伏笔回收和 Narrative Loss Budget；关键删除必须人工确认 |
| 剧本 | 08 分集与分场规划 | 通过的改编方案 | `EpisodeOutline`、`SceneCard`；明确每集钩子、冲突、高潮和悬念 |
| 剧本 | 09 Adaptation Writer：剧本写作 | 当前集大纲、Canon | 可生产分场剧本；包含场景、动作、对白、旁白、声音和入口/出口状态，不写最终视频 Prompt |
| 剧本 | 10 Script QA 与锁定 | 分场剧本、Canon、时长目标 | `EpisodeScript`；检查完整性、动机、时间线、对白长度和动漫可表现性 |
| 前期 | 11 Director Planner | Episode Script、Story Bible、媒介规范 | `DirectorRead`、连续 `Storyboard`；锁定景别、构图、方向、走位、剪辑、时长和声音意图 |
| 前期 | 12 Art Director / Asset Designer | Storyboard、Canon、媒介规范 | `ArtBible`；角色比例/转面/表情/口型/手势、服装、道具、色彩脚本、场景和背景风格 |
| 前期 | 13 美术资产生成与设定锁定 | Art Bible、模型能力 | 不可覆盖 `AssetVersion`；身份、线条、配色、结构和可动画性通过后晋级 |
| 前期 | 14 Layout | Storyboard、锁定资产 | 正式 `Layout`；角色尺寸、透视、站位、视线、轴线、背景范围、前景遮挡和二维摄影空间 |
| 前期 | 15 Animatic、时长和临时声音锁定 | Storyboard、Layout、临时声音 | `Animatic`、`TimingSheet`、`DialogueTiming`、`AnimationEffortPlan`；节奏未锁定不得开原画 |
| 中期 | 16 Animation Director | Layout、Animatic、Timing、角色设定 | `KeyAnimationPlan`；关键姿势、动作弧线、停帧、预备/跟随动作、一拍二/局部一拍一和爆发作画位置 |
| 中期 | 17 关键帧原画 | Key Animation Plan、角色设定 | `KeyAnimation`；角色结构、轮廓、表情、重心和动作方向必须先成立 |
| 中期 | 18 原画检查与修正 | 原画、Layout、动作计划 | 检查走形、姿势力量、重心、动作可读性和帧间连接；不合格只返修原画 |
| 中期 | 19 中间画、清稿与上色 | 通过的原画、Timing Sheet、色彩设定 | `Inbetween`、`CleanCel`、`ColoredCel`；帧数由 Timing 决定，不把越流畅等同于越好 |
| 中期 | 20 背景绘制 | Layout、色彩脚本、场景设定 | `BackgroundPlate`、前景和移动层；保持透视、光照和绘制体系一致 |
| 中期 | 21 二维摄影与镜头合成 | 上色角色、背景、前景、特效、摄影表 | `CompositeShot`；分层平移、推近、背景卷动、多平面和特效必须服从分镜 |
| 中期 | 22 正式配音与声音同步 | 锁定时长、Dialogue Timing、画面 | 正式对白、旁白、音效和音乐；校准口型、停顿、动作落点和声音透视 |
| 审片 | 23 镜头审片与 Canon 对账 | Storyboard、Layout、Timing、真实性规范、合成镜头 | `TakeReview`；剧情、结构、动作时机、背景、摄影、声音和连续性全部验收 |
| 返修 | 24 Repair Advisor | 失败证据、工序血缘、历史尝试 | 将任务退回分镜、Layout、原画、中间画、上色、背景、摄影或声音的真实出错处 |
| 连续性 | 25 连续性检查与下一镜 | 通过镜头、前后 Shot Spec | `AcceptedTake` 和真实出口状态；只有已接受结果能成为下一镜父源 |
| 后期 | 26 序列质检与剪辑 | Accepted Takes、Animatic | 场/集剪辑；检查节奏、信息、动作和声音衔接 |
| 后期 | 27 后期合成 | 剪辑序列、声音和字幕资产 | 最终特效、调色、字幕、对白、音效、音乐、响度、片头片尾和母版 |
| 交付 | 28 Delivery、数据回流与回归 | 成片母版、平台要求 | 格式/帧率/字幕/声音/命名检查；记录返修率、成本、人工修改和最终采用数据 |

`AnimationEffortPlan` 将镜头分为 A 级高潮/爆发作画、B 级标准角色表演、C 级有限动画、D 级叙事蒙太奇或插入镜头。所有等级都必须重新设计 Layout、构图、关键姿势和时间；等级降低的是中间画数量和运动复杂度，不是角色设计质量。

### 7.3 上下文装配原则

1. 任何 Agent 都不默认读取整本小说；Novel Analyst 使用局部抽取、章节汇总、全书合并。
2. 原文保留稳定章节、段落和句子 ID，任何事实都能回到证据。
3. 后续 Agent 主要读取 Story Bible、当前集、当前场和必要原文证据。
4. Director Planner 可读取全剧主线和相邻剧情摘要；Animation Director 读取当前场和镜头的 Layout、Animatic、Timing、角色设定与表演意图。
5. 图片、视频或二维专项执行模型只读取当前工序的最小任务包，不读取整本小说和无关聊天记录。
6. 用户修改、原著事实、模型推断和改编决定分别标记；被拒绝候选不能进入 Canon。
7. 所有调用固定输入版本，不允许只引用“最新版”。

### 7.4 AI 执行底座的边界

`Generation Router`、`Prompt Compiler`、生成前门禁和 `Executor` 是贯穿工序的底层能力，不是动画制作阶段。它们负责选择当前工序的 Provider、装配最小上下文、编译 Prompt 和参考职责、校验 Schema/版本/授权/预算，并记录异步任务、候选、成本、重试和审计。

任何模型调用都必须服务于一个明确二维工序。禁止从 Episode Script 或 Storyboard 绕过 Layout、Timing 和原画计划直接批量生成最终镜头。

## 8. 火山剧创类产品可借鉴机制

以下“已验证细节”核对自火山剧创官方的[短剧创作](https://docs.volcengine.com/docs/87744/2261814?lang=zh)、[资产管理](https://docs.volcengine.com/docs/87744/2261816?lang=zh)和[新功能发布记录](https://docs.volcengine.com/docs/87744/2261810?lang=zh)（核对日期：2026-08-19）；“本项目实现”是基于这些机制做的工程化设计，不代表官方内部实现。

### 8.1 值得模仿的不是页面，而是生产闭环

| 官方已验证的产品细节 | 解决的问题 | 本项目核心实现 |
|---|---|---|
| Agent 模式按剧本、设定、分镜、视频推进，同时保留人工模式 | 自动化与可控编辑不能二选一 | 同一状态机支持 `AUTO`、`ASSISTED`、`MANUAL`，人工修改仍走版本和门禁 |
| 显示剧本解析、分镜解析状态 | 长任务不可黑盒 | 展示当前节点、输入版本、等待依赖、预计成本、失败码和可恢复动作 |
| 角色/场景/道具可 AI 生成、资产库选择或本地上传 | 资产来源多，但下游必须统一引用 | 所有来源先转成统一 `AssetVersion`，审核并晋级后才能生成视频 |
| 个人资产、团队资产、写实人像分域；支持文件夹、批量上传、搜索和权限 | 复用、协作、授权隔离 | 资产作用域、来源、授权期限、审核状态和可见范围进入数据契约 |
| 提示词支持素材多选、`@` 精准引用、粘贴后按名称自动关联、自动保存 | 降低素材绑定和提示词丢失风险 | 编辑器展示“素材职责芯片”；名称只用于检索，提交时写不可歧义的资产版本 ID |
| 单分镜一次最多生成 4 个候选 | 抽卡效率和横向比较 | 候选组共享同一 Shot Spec，只允许一个受控变量变化，并保存选择理由 |
| 首尾帧、从视频截图作为下一镜参考、视频续写 | 提升跨镜连贯性 | 将上一镜验证后的出口帧和状态自动绑定为下一镜入口，续写仍受最长链路限制 |
| 超分、字幕擦除、视频编辑、延长 | 避免整镜重生 | Repair Router 按失败码选择局部编辑、后处理、延长、桥接或重生 |
| 单集批量切模型、批量生成、合并下载 | 提升高产操作效率 | 批处理先做影响预览和预算预估，再生成不可变批次清单，可暂停和恢复 |
| 消耗可按团队、项目、单图、单视频追踪 | 不能只看 API 成功率 | 成本绑定 Attempt、候选和最终采用秒数，计算可用秒成本 |

### 8.2 分镜工作台应暴露的关键细节

一个分镜工作区同时呈现且不藏进多层弹窗：

- 左侧：集/场/镜头树、状态、失败标记和依赖关系。
- 中间：当前候选、首尾帧、时间轴、裁切安全区和相邻镜头出口/入口对照。
- 右侧：Shot Spec、素材职责、生成模式、模型参数、Prompt 编译结果和验收项。
- 候选区：同规格候选并排、失败证据、成本、耗时、版本差异和入选理由。
- 操作区：重试、局部编辑、拆镜、续写、替换入口帧和转人工；操作前显示会改变的唯一主变量。

必须支持“从结果追溯到原因”：成片镜头 -> 候选 -> Attempt -> Generation Request -> Prompt Compiler 版本 -> Shot Spec -> Story/Scene 状态和资产版本。没有这条链路，问题只能靠猜。

### 8.3 比同类产品更应加强的部分

- 自动解释为什么选择该生成模式、模型和素材，而不是只显示最终 Prompt。
- 提交前显示可生成性风险：人物过多、动作过密、台词超时、首尾状态冲突、参考素材职责冲突。
- 相邻镜头并排显示连续性 Diff，明确哪些字段继承、改变或未知。
- 候选生成采用受控实验，不把四个完全不同的 Prompt 混在一组。
- 修复前显示根因证据、预计成本、预计提升和回滚点。
- Agent 自动化可以随时切到人工模式，但状态、版本、预算和审计不能失效。

## 9. 图片生成控制（二维生产辅助）

图片模型用于美术设定、Layout 候选、原画辅助、背景和局部修复。生成结果只有通过对应工序门禁后才能晋级；单张完成插画不能直接充当整段二维动画。

### 9.1 资产必须是版本化资产包

角色资产：

- 独立清晰的身份面部锚点。
- 中性全身图。
- 每套剧情服装的独立版本。
- 表情和姿态参考独立保存。
- 项目画风锚点。
- 已失败的参考组合和失败原因。

场景资产：

- 空间主视图和反向机位。
- 门、窗、桌、楼梯等空间锚点。
- 日、夜和特殊光线状态。
- 人物站位区域、轴线和出入口。

道具资产：

- 尺寸、材质、颜色、持有人和当前状态。
- 破损、打开、关闭等剧情版本。
- 关键道具使用独立主体图。

注意：资产库可以保存人物多个角度，但向 Seedance 2.0 提交人物参考时不要将多视图拼在一张图中。官方资料指出，多视图可能被识别为多个主体并加剧身份漂移。优先使用独立大头照和独立全身照。

### 9.2 图片模型按能力路由

| 需求 | 路线 |
|---|---|
| 精确局部编辑、图层拆分、透明素材 | Seedream 5.0 Pro 类能力 |
| 角色/分镜连续组图 | Seedream 5.0 Lite、4.5 类组图能力 |
| 换装、多图融合、风格迁移 | 支持多图参考和精确编辑的模型 |
| 视频首尾关键帧 | 图片编辑/多图融合模型 |
| 快速探索草图 | 低成本图片模型 |

路由输入必须先描述能力需求，再选择模型，不允许只写固定模型名。

### 9.3 图片 Prompt 结构

```text
任务类型和图片用途
主体及不可变特征
服装和道具
动作或姿态
场景与空间关系
构图、景别和视角
光线、色彩和风格
需要修改的内容
必须保持不变的内容
输出约束
```

官方 Seedream 指南的核心规则：

- 使用自然语言明确“主体 + 行为 + 环境”。
- 风格、色彩、光影和构图只在需要时补充。
- 图片编辑必须说明修改对象和保持不变的部分。
- 多图输入必须说明每张图承担什么职责。
- 生成视频关键帧时写明“影视视频静帧画面”。
- 图片宽高比与目标视频保持一致，避免裁切和变形。

### 9.4 图片资产晋级

```text
DRAFT -> CANDIDATE -> VERIFIED -> PROMOTED -> LOCKED
```

- `DRAFT`：探索结果，禁止进入视频生产。
- `CANDIDATE`：自动检查通过，可参与候选比较。
- `VERIFIED`：身份、服装、风格、结构和构图通过。
- `PROMOTED`：正式绑定到剧情状态。
- `LOCKED`：已有镜头依赖，只能创建新版本，不能覆盖。

硬门禁：

- 尺寸和宽高比正确。
- 主体数量正确。
- 身份、服装、关键配饰和道具匹配。
- 无异常文字、Logo、水印和拼接残留。
- 肢体结构可接受。
- 作为视频关键帧时存在可继续运动的姿态和安全构图。

身份错误不能被高审美分抵消，硬门禁失败不得晋级。

## 10. 视频生成控制（可选镜头 Provider）

本节定义 Seedance、AniSora 等视频 Provider 的受控调用方式，不代表主生产线可以从分镜直接跳到视频。只有在 Storyboard、Art Bible、Layout、Animatic/Timing 和 Key Animation Plan 已准备完成，并且当前镜头明确选择生成式镜头路线时，才允许调用视频模型。模型是镜头执行器，不是导演、动画监督或完整生产线。

### 10.1 先选模式，再写 Prompt

| 镜头条件 | 生成模式 |
|---|---|
| 无固定角色的环境、氛围或抽象过场 | 文生视频 |
| 起始构图必须准确，结束状态开放 | 首帧生视频 |
| 起点和终点必须准确 | 首尾帧生视频 |
| 需要复用人物、风格、动作、运镜或音色 | 全模态参考 |
| 原视频大部分正确，只需局部修改 | 视频编辑 |
| 需要沿用已有画面继续发展 | 视频延长 |
| 多段视频之间缺少过渡 | 轨道补全或桥接镜头 |

### 10.2 Seedance 2.5 关键能力和边界

- 单次输出支持 4-30 秒。
- 全模态参考最多 30 张图片、10 段视频、10 段音频，总上限 50 个素材。
- 最多 10 段参考视频，总时长不超过 30 秒。
- 最多 10 段参考音频，总时长不超过 30 秒。
- 支持时间戳描述和长叙事，但复杂镜头仍需经过可生成性检查。
- 编辑、延长、首帧和首尾帧任务的宽高比由输入素材决定。
- MOV 输出适合调色、抠像和后期合成。

任务提交前强制预校验：

```text
视频编辑：
  reference_video >= 1
  待编辑视频时长 4-30 秒
  ratio = adaptive
  duration = -1
  Prompt 明确包含编辑、增加、删除、修改或替换意图

视频延长：
  reference_video >= 1
  ratio = adaptive
  duration = 4-30 或 -1
  Prompt 明确包含向前/向后延长、延续或续写意图

首帧/首尾帧：
  first_frame = 1
  last_frame = 0 或 1
  ratio = adaptive
  输出比例跟随首帧
```

Seedance 2.0 仍需兼容时，核心限制是参考素材更少：图片最多 9 张，视频和音频分别最多 3 段，参考音/视频总时长通常不超过 15 秒。

### 10.3 可生成性复杂度

```text
complexity =
  2 * 同镜头主体数
  + 2 * 人物交互数
  + 动作阶段数
  + 运镜变化数
  + 场景状态变化数
  + 2 * 变装或变形次数
  + 台词角色数
  + 特效层数
```

初始策略：

- `0-5`：直接生成。
- `6-8`：优先首尾帧或增加动作参考。
- `9-11`：拆成两个片段。
- `>=12`：重新设计镜头，禁止自动批量提交。

阈值属于项目初始策略，需要按题材和模型版本用真实通过率校准。

### 10.4 视频 Prompt 结构

```text
素材职责
入口状态
按时间分段的动作
镜头运动
出口状态
声音和台词
风格与画质
禁止项
```

示例：

```text
@图片1只参考林夏的面部和发型，@图片2参考灰色职业套装，@图片3参考会议室布局。
保持角色身份、服装、会议室空间和冷白光线一致。

[0-2 秒] 中景固定镜头。林夏把蓝色文件夹平放到桌面，右手留在文件夹上。
[2-4 秒] 镜头缓慢推进。画面右侧一只手将旧照片推入画面，林夏低头看照片。
[4-6 秒] 推进到近景。林夏抬眼看向画面右侧，表情由克制变为警惕。

出口状态：照片停在文件夹上方，林夏右手仍按住文件夹，视线朝右。
无额外人物，无字幕、文字、Logo 和水印；不改变服装和场景布局。
```

编译检查：

- 每份素材只有明确职责。
- 时间段总和与目标时长一致。
- 动作有可见开始和结束状态。
- 不出现互相矛盾的动作或运镜。
- 台词能在分配时间内自然说完。
- 出口状态能够作为下一镜入口。

Prompt Compiler 还必须遵循以下边界：

1. 内部可以使用 JSON/YAML 组织状态，但发送给生成模型的最终 Prompt 默认是自然语言，不能泄露 `power_shift`、`hidden_want`、`subtext` 等内部标签。
2. 有已接受视频作为参考时，视频负责携带人物外观、构图、动作相位和环境；文字只写参考职责、本镜头变化量、已完成/保留节拍和终点。
3. 有已接受尾帧但没有视频时，尾帧只携带静态状态，文字补充运动方向、运镜相位和音频相位。
4. 没有视觉父源时，才在文字中完整描述必要入口状态。
5. 最终 Prompt 只能描述当前镜头，不包含整集未来剧情，不把计划终点写成已经发生的事实。

固定编译顺序：

```text
精确素材标签和职责
  -> 父源与实际入口状态
  -> 本镜头唯一叙事动作
  -> 可见/可听的表演载体
  -> 镜头、光线、环境和声音
  -> 连续性锁定项
  -> 已完成节拍排除
  -> 后续保留节拍排除
  -> 明确停止终点
```

超过 Prompt 预算时，先删除泛化风格词、重复形容词、未来剧情、参考素材已经明显携带的背景描述和次要动作；不能先删除素材职责、当前动作、终点和高风险连续性锁。

### 10.5 表达控制：先编译表演，再编译 Prompt

视频质量问题经常不是模型参数问题，而是“剧情意图 -> Prompt”跳得太快。中间必须有 `ExpressionSpec`：

编译表演前先分轨：

- **叙事轨**：有人物或主体在追求、抵抗、选择、隐瞒、反应或发生关系变化，需要完整 Director's Read。
- **非叙事轨**：纯产品展示、材质/灯光测试、功能演示、动作/运镜迁移、抽象特效或环境氛围，只保存 `utility_intent` 和 `non_narrative_refusal`，不得为了填字段虚构人物欲望和冲突。

叙事轨的内部判断至少包括：剧情功能、可见的前后价值变化、POV、权力/主动权变化、目标、阻碍与策略、潜台词矛盾、被压住但可拍到的行为、不可迁移的项目细节，以及明确拒绝的套路化处理。它们是内部导演记录，最终 Prompt 只能输出可拍摄或可听见的载体。

```json
{
  "dramatic_function": "林夏发现对方掌握了旧照片，但不愿暴露慌乱",
  "audience_knowledge_delta": "确认照片会威胁林夏",
  "subtext": "表面镇定，实际开始防御",
  "emotion_curve": [
    {"at": 0.0, "state": "controlled", "intensity": 0.25},
    {"at": 0.55, "state": "recognition", "intensity": 0.55},
    {"at": 1.0, "state": "guarded", "intensity": 0.72}
  ],
  "performance_beats": [
    {"action": "press_folder", "micro_action": "thumb_tightens", "gaze": "photo"},
    {"action": "lift_eyes", "micro_action": "jaw_sets", "gaze": "offscreen_right"}
  ],
  "blocking": {"screen_position": "center_left", "screen_direction": "looks_right", "prop_hand": "right"},
  "camera_motivation": "在她认出照片后推进，让观众读到被压住的警惕",
  "sound_focus": ["folder_contact", "room_tone_drop", "single_breath"],
  "cut_on": "eyes_lock_offscreen_right"
}
```

编译顺序固定为：

```text
剧情功能
  -> 观众在本镜头新增什么认知
  -> 潜台词和情绪曲线
  -> 可见的动作、微表情、视线和身体张力
  -> 走位、轴线、道具手和空间关系
  -> 有叙事动机的景别与运镜
  -> 声音焦点、留白和剪切点
  -> 模型专用时间码 Prompt
```

硬规则：

- “悲伤、紧张、电影感”不能单独作为表演指令，必须落成眼神、呼吸、手部、姿态和动作速度。
- 运镜必须说明叙事动机；没有动机时优先固定镜头，避免无意义环绕、推拉和晃动。
- 一个动作节拍只包含一个主要状态变化；反应、决定、执行通常拆成不同节拍。
- 台词镜头先按实际音频或语速估算时长，再安排动作；不能让模型在不足时长里硬塞对白。
- 双人以上场景先锁轴线、视线目标、左右位置和道具手，再写人物动作。
- 高价值信息用特写、反应镜头或插入镜头表达，不要求一个长镜头同时交代所有信息。
- 出口状态必须包含下一镜真正需要继承的事实，不把“情绪更强”这种模糊描述当连续性状态。

### 10.6 受控候选和序列表达

同一候选组保持 Shot Spec、资产、模型和基础参数一致，只改变一个实验维度：

| 候选组目的 | 允许变化 | 不允许同时变化 |
|---|---|---|
| 表演选择 | 微表情强度、停顿、动作速度 | 机位、人物位置、服装 |
| 镜头选择 | 固定/缓推/轻跟、景别落点 | 表演节拍、台词、资产 |
| 光线选择 | 主光方向或明暗比 | 构图、动作、画风 |
| 修复验证 | Repair Plan 指定的唯一主变量 | 其他 Prompt 和参数 |

候选比较不能只看单镜头“漂亮程度”，必须同时看：剧情信息是否清楚、表演是否可信、出口状态是否可剪、与前后镜是否一致、是否引入额外修复成本。

序列级表达控制：

- 为场景定义景别节奏，避免连续多个同尺寸人物镜头造成机械感。
- 关键台词前留反应或环境呼吸，重要信息后保留可剪的停顿帧。
- 镜头入口和出口各保留稳定动作区，具体余量由题材回归数据校准，不能固定套一个秒数。
- 动作跨剪时记录动作相位、肢体、道具手和运动方向；声音可用 J-cut/L-cut 连接，但必须在时间轴契约中显式声明。
- 同一场景完成后做色彩、环境声、音色、响度和字幕区域统一，再进入整集粗剪。

### 10.7 关键帧和时长策略

自动选择首尾帧的条件：

- 镜头必须结束在指定姿态或构图。
- 人物需要换装、转身、进出画面或跨场景。
- 商品、道具存在明确的打开/关闭等状态变化。
- 运镜必须落到指定人物、建筑或道具。
- 强风格画面需要全程保持一致。

Seedance 2.5 支持 30 秒不代表所有镜头都应生成 30 秒：

- 单一连续表演或不可切断运镜可以尝试长生成。
- 情绪特写和反应镜头优先短生成。
- 多角色轮流说话、多次场景变化和多段动作默认拆镜。
- 长镜头只有在回归数据证明通过率更好时才能成为默认路线。

### 10.8 样片模式和异步任务

- 正式高成本生成前先用 Draft/低成本样片验证构图、动作、镜头调度和 Prompt 意图。
- 图片和视频任务均视为异步任务。
- 生产环境使用 Webhook 接收状态，轮询作为兜底。
- 任务状态、供应商任务 ID、错误码和回调事件全部持久化。
- 超时不能直接判定失败，应先查询供应商最终状态。

## 11. 质量检查与自动返修

### 11.1 三层检查

确定性检查：

- 分辨率、比例、时长、编码和文件完整性。
- 黑帧、冻结帧、重复帧和闪烁。
- 音轨、响度、削波和异常噪音。
- 水印、异常文字和审核状态。

单镜头语义检查：

- 人物身份、数量、服装和道具。
- 动作是否完成，方向是否正确。
- 表情、景别、运镜和焦点。
- 台词角色、发音、音色和口型。
- 首尾状态是否符合 Shot Spec。

跨镜头连续性检查：

- 人物进出方向和视线。
- 服装、伤势、手持道具和空间位置。
- 光线、天气和时间状态。
- 动作衔接和稳定切点。
- 音色、环境声和音乐连续性。

### 11.2 评价器隔离

- 生成 Agent 不能给自己的结果最终放行。
- Evaluator 不读取生成 Agent 的自我解释。
- VLM 评价必须输出时间码、区域或检测证据。
- 自动阈值使用人工标注集校准。
- 每批作品随机人工复核，监控评价器漂移。

评价报告必须把硬门禁和软评分分开，不能用总分掩盖身份或连续性错误：

```json
{
  "candidate_id": "vid_014_c03",
  "hard_gates": {
    "identity_match": true,
    "subject_count": true,
    "asset_state_match": true,
    "rights_and_compliance": true,
    "output_integrity": true
  },
  "scores": {
    "story_clarity": 0.88,
    "performance": 0.81,
    "motion": 0.84,
    "camera_intent": 0.79,
    "audio": 0.9,
    "editability": 0.86,
    "continuity": 0.92
  },
  "evidence": [
    {"at_ms": 4300, "region": "face", "check": "emotion_end", "observed": "guarded"}
  ],
  "decision": "PASS",
  "failure_codes": [],
  "evaluator_version": "shot-eval.v4"
}
```

放行条件至少包括：全部硬门禁通过；关键维度达到镜头等级阈值；入口/出口状态通过；不存在比当前候选更低成本且明显更好的修复路线。阈值按题材、镜头类型和模型版本分别校准。

### 11.3 失败码和修复动作

| 失败码 | 首选动作 |
|---|---|
| `IDENTITY_DRIFT` | 换独立身份锚点、减少人物参考、重要素材前置 |
| `STYLE_DRIFT` | 先统一关键帧风格，再使用首尾帧或风格参考 |
| `SUBJECT_COUNT_ERROR` | 减少同镜头人物或先生成分组画面 |
| `ACTION_INCOMPLETE` | 缩短动作链、调整时长或拆镜 |
| `MOTION_CONTRADICTION` | 删除冲突动作和运镜约束 |
| `END_STATE_MISMATCH` | 生成尾帧并切换首尾帧模式 |
| `LOCAL_OBJECT_ERROR` | 使用视频编辑局部修复 |
| `EXTENSION_JUMP` | 明确入口状态，生成桥接或轨道补全 |
| `TEXT_OR_WATERMARK` | 清理输入文字，字幕擦除或局部编辑 |
| `SPEECH_MISMATCH` | 缩短台词、拆说话镜头、重新绑定音色 |
| `MOTION_COMIC_DRIFT` / `PANEL_PAN_ZOOM` | 退回 Layout 和动画计划，禁止继续用漫画格推拉 |
| `CUTOUT_PUPPET_DRIFT` / `PARALLAX_ONLY` | 重做关键姿势和局部动作层，不以骨骼摆动或纯视差冒充二维作画 |
| `CEL_SHADED_3D_DRIFT` | 退回美术/原画，恢复手绘线条、赛璐璐阴影和二维空间语言 |
| `LINE_BOIL` / `FACE_MELT` | 退回原画或清稿，锁定结构与线条后再补间 |
| `OVER_ANIMATION` / `CAMERA_FLOAT` | 减少无意义连续运动，按 Timing Sheet 恢复停帧和有动机的摄影 |
| `TEXTURE_FLICKER` | 退回上色、背景或摄影层，定位闪烁来源并只重做失败层 |

Seedance 官方常见问题的核心处理原则：

- 人脸漂移：独立人脸参考、主体定义清楚、重要素材靠前。
- 风格漂移：输入参考图先统一成目标画风。
- 双胞胎或人数错误：减少同镜头人物，不使用人物多视图拼图。
- 特效不准确：提供目标特效视频作为动作/效果参考。
- 延长跳变：明确入口、出口，必要时改为首尾帧或桥接镜头。
- 延长画质持续劣化：不要无限续写模型产物，重新建立高质量入口素材。

### 11.4 Take 判定与 Canon 对账

每个候选不能只有“通过/不通过”，先做五类生产判定：

| 判定 | 适用条件 | 系统动作 |
|---|---|---|
| `KEEP` | 核心叙事和硬门禁通过 | 进入候选选择和 Canon 对账 |
| `FIX_IN_POST` | 问题属于调色、字幕、混音、裁切或少量头尾帧 | 保留视频，创建后期任务，不消耗新生成次数 |
| `EDIT_SOURCE` | 构图与时序正确，只有一个局部层错误且供应商支持视频编辑 | 以当前候选为源，只修改失败层 |
| `REROLL` | Prompt 正确，问题更像采样偶然性 | Prompt 和输入不变，只换 seed；有限次数 |
| `REWRITE` | 同一问题在两个以上候选重复出现 | 判定为系统性问题，改 Prompt、模式或拆镜 |

`KEEP` 不等于立即锁定。Take Review 必须记录实际首尾状态、完成/未完成/意外提前完成的节拍、连续性破坏、可接受偏差、置信度和不确定项。只有对账完成后才允许更新 Canon：

- `ACCEPT`：真实状态成为下游父源。
- `ACCEPT_WITH_DEVIATION`：保存偏差，更新后续节拍和下一镜入口。
- `REPAIR`：不推进 Canon，先完成局部编辑、补尾或其他修复。
- `REJECT`：不更新 Canon，禁止作为后续参考源。

### 11.5 最小修改和停止条件

每次重试只能改变一个主要变量，例如参考资产、生成模式、动作拆分、时长或模型。

同一 Prompt 换 seed 是纯重抽；保持 seed 只改一个 Prompt 子句最接近受控实验。一次同时修改 Prompt、素材、模式和时长会让结果无法归因，生产台账应拒绝将其标为有效 A/B 实验。

自动循环在以下任一条件停止：

- 达到镜头最大尝试次数。
- 达到镜头预算。
- 连续两次提升低于最小收益。
- 相同失败码连续出现且无新策略。
- 需要修改锁定剧情或正式资产。
- 涉及审美、授权或剧情含义变化。

停止后进入人工选择、批准拆镜、提高预算或修改创作意图。

## 12. 模型路由

```text
候选模型
  -> 过滤不支持任务类型、时长、比例和素材数量的模型
  -> 过滤不满足合规和预算的模型
  -> 按历史通过率、连续性、成本、耗时和可编辑性排序
  -> Agent 只处理规则无法表达的特殊创作需求
```

路由决策至少读取：

- 镜头类型、价值等级和复杂度。
- 人物、动作、台词和参考素材数量。
- 是否要求精确首帧或尾帧。
- 同类镜头在各模型版本上的历史通过率。
- 剩余预算、队列时间和交付期限。
- 后期是否需要 MOV、透明素材或图层。

每次尝试必须记录：

- Shot Spec、Agent、Router 和模型版本。
- Prompt、参数和资产版本。
- 供应商、任务 ID、耗时和成本。
- 自动评分、人工评分和失败码。
- 是否入选最终成片。

## 13. 生产指标

```text
一次可用率 = 首轮通过镜头数 / 首轮生成镜头数
平均生成倍数 = 总生成视频秒数 / 最终采用视频秒数
可用秒成本 = 总生成成本 / 最终采用视频秒数
返修率 = 返修镜头数 / 总镜头数
人工分钟/成片分钟 = 人工操作分钟 / 成片分钟
自动完成率 = 无人工修改通过的镜头数 / 总镜头数
连续性错误率 = 连续性失败镜头数 / 总镜头数
```

不能用任务成功率代替成片可用率，也不能用单次 API 单价代替可用成片成本。

## 14. 回归与发布门禁

黄金回归集至少覆盖：

- 单人情绪变化。
- 双人正反打。
- 走位和拿取道具。
- 服装或状态变化。
- 多人场景。
- 快速动作和特效。
- 首尾帧运镜。
- 视频编辑和延长。
- 有声对白。
- 2D、3D 和写实画风。

以下任何升级都必须跑回归：

- Agent 系统提示词和模型版本。
- Prompt Compiler 模板。
- Generation Router 策略。
- 图片和视频模型版本。
- 评价器、阈值和权重。
- 图片预处理和视频后处理。

回归门禁分成三类，不能混为一个总分：

| 门禁 | 检查内容 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| 结构门禁 | JSON Schema、重复键、枚举、必填字段、Prompt 格式 | 数据可被系统安全消费 | 剧情和画面质量 |
| 语义门禁 | 父子血缘、循环、状态权威、Take 对账、素材职责、节拍范围、连续性锁 | 工作流没有明显逻辑矛盾 | 模型一定按要求执行 |
| 真实效果门禁 | 固定输入生成、自动检测、盲评和人工复核 | 当前模型版本的实际可用率 | 未来模型或其他题材效果 |

Prompt 静态评分只用于阻止结构退化、通用套话、动作反转、引用角色错绑和相互矛盾的指令。它不能宣称提示词有创意，也不能替代真实视频生成、母语审校和人工导演判断。

发布流程：

```text
历史任务离线回放
  -> Shadow 运行
  -> 少量 C 级镜头 Canary
  -> B 级镜头放量
  -> 最后开放 A 级钩子、高潮和集尾镜头
```

## 15. 实施顺序

### P0：先保证系统不会乱

1. `NovelSource`、`SourceIndex`、`NovelCanon`、`StoryBible` 和原文证据链。
2. `FaithfulnessProfile`、`NarrativeLossBudget`、`StoryCoverageLedger` 和删除审批门禁。
3. `ProductionMediumProfile`、`AnimeAuthenticityProfile`、`ArtBible` 与正式资产版本。
4. Episode Script、Storyboard、Layout、Animatic、Timing Sheet 和 Key Animation Plan 的最小 Schema。
5. `project-state.v1`、`clip-contract.v1`、`prompt-spec.v1`、`take-review.v1`、`generation-run.v1`。
6. Planned/Observed/Accepted Canon 的权威顺序，以及 Scene/Shot 父子血缘、重锚和连续生成深度。
7. 八类 Agent 的调用、数据、写权限和结果契约；所有 Agent 只提交 Patch Proposal。
8. Novel Canon、Story Coverage、Project State、Continuity、工序依赖和 Take Reconciliation Validator。
9. 图片、视频和二维专项 Provider 的任务类型、参数、许可和输入版本校验器。
10. 美术、Layout、原画、中间画、上色、背景、摄影和声音产物的晋级与锁定。
11. 单镜头硬门禁、二维真实性失败码，以及 `KEEP/FIX_IN_POST/EDIT_SOURCE/REROLL/REWRITE` 判定。
12. 失败到真实工序的返修路由、有限重试、预算、幂等和停止条件。
13. 事件追加、Outbox/Inbox、恢复态和 Temporal 断点续跑。
14. 人工操作与 Agent 操作共用命令、事件和审计链路。

### P1：提高一次可用率

1. 长篇小说分层分析、冲突合并和上下文最小装配。
2. 改编覆盖率、伏笔回收、人物弧光和 Narrative Loss 自动检查。
3. Layout/Xsheet/Timing 工作台和 A-D 作画等级分配。
4. 关键姿势、角色结构、动作时机和可读性评价。
5. 中间画、线稿、上色、背景与二维摄影专项 Provider 适配器。
6. 镜头复杂度评分、自动拆镜、图片模型和视频模式路由。
7. Repair Advisor 按工序最小修改策略和跨镜头连续性检查。
8. Draft/Animatic/样片验证路径。
9. ExpressionSpec、受控候选和序列级表达检查。
10. Source-Carries-State Prompt Compiler、Prompt 压缩和中英文静态门禁。

### P2：形成长期优势

1. 题材级模型路由统计。
2. 自动评价器与人工评分校准。
3. 黄金回归集和发布门禁。
4. 发布数据反哺剧情和镜头策略。
5. 团队、题材和模型版本的成功策略库。

当前最重要的不是继续增加功能，而是完成 P0，让已有基础功能进入可控、可追踪、可恢复的生产闭环。

## 16. Seedance 2.0 Skill OS 融合结论

本节研究对象为 [Emily2040/seedance-2.0](https://github.com/Emily2040/seedance-2.0)，核验版本为主分支 `v6.7.0`、提交 `44b5149`，本地核验日期 2026-08-19。该仓库采用 MIT License。

### 16.1 定位判断

它不是可直接部署的视频生成 Web 产品，也没有替代本项目的前端、业务 API、数据库、Temporal 工作流、资产系统或供应商适配器。它是一套围绕 Seedance 的 Agent Skill OS，主要提供导演方法、Prompt 编译规范、序列状态契约、连续性检查、Take Review、评测和发布校验。

因此融合策略是：**吸收生产内核和可执行契约，不 Fork 成产品，不把其技能目录直接当业务架构。**

### 16.2 采用、改造与不采用

| 分类 | 内容 | 本项目处理 |
|---|---|---|
| 直接借鉴思想 | 全局规划、逐镜生成、Observed State 覆盖 Planned State、参考素材携带状态、单变量返修 | 写入核心状态机和生产规则 |
| 改造后采用 | 5 类 Schema、镜头血缘、连续性链、Prompt Compiler、Take Review、Generation Run | 扩充集数、资产、权限、授权、供应商、成本和中文规则后实现 |
| 作为测试素材 | 连续镜头样例、黄金 Prompt、失败用例、静态评分语料 | 转为本项目 fixtures 和黄金回归集 |
| 仅作知识参考 | 摄影、灯光、动作、音频、风格、版权等 28 个子技能 | 由 Prompt Compiler 按需检索，不拆成 28 个物理 Agent |
| 明确不采用 | 技能安装器、README 视觉资产、面向多 Agent 客户端的安装兼容层 | 与剧创工坊业务无关，不引入工程包袱 |
| 不能直接相信 | 模型 ID、价格、素材上限、平台能力和第三方 Provider 参数 | 始终以当前官方文档和账号控制台为准，进入版本化能力表 |

### 16.3 已核验的工程成熟度

在独立临时目录和 CPython 3.12 环境中运行该仓库锁定依赖及完整 archive-safe 校验，结果为：

- 19/19 项仓库校验通过。
- 根技能和 28 个子技能结构通过。
- 5 个 JSON Schema 均执行了声明实例。
- 126 条评测用例、47 条序列用例通过结构校验。
- 共运行 1224 个单元测试，整体结果为 `OK`；其中 65 个因当前 Windows 权限、缺少 FFmpeg 或属于 POSIX 专用语义而跳过。
- 其确定性提示词架构压力测试中，完整 `skill_formula` 平均 3.92/4，通过其发布门槛；简化教程式为 3.05，普通网络式为 2.06。

这些结果说明它的状态契约、输入防错、Prompt 结构和连续性校验具有较高参考价值。它们不证明真实 Seedance 成片质量，不证明多语言文案质量，也不能替代本项目面向中文短剧、Seedance 2.5 和真实供应商 API 的回归。

### 16.4 许可和代码复用边界

- 方法、字段设计和测试思路可以融合进本项目。
- 若直接复制其 Python、Schema 或较大段文档，应保留 MIT 许可证及版权声明，并在第三方清单中记录来源、原提交和修改说明。
- 优先围绕本项目领域模型重写核心实现，只移植确有价值且已有深度测试的 Validator；不要整仓复制。
- 上游未来升级不自动合并。先做差异评审，再用本项目黄金回归集验证。

## 17. 依据与参考

### Agent 与工作流

1. [方舟 Managed Agents 概述](https://docs.volcengine.com/docs/82379/2553713?lang=zh)
2. [方舟 Multi Agent](https://docs.volcengine.com/docs/82379/2553730?lang=zh)
3. [Temporal Workflow](https://docs.temporal.io/workflows)

### 图片生成

1. [图片生成 API](https://docs.volcengine.com/docs/82379/1541523?lang=zh)
2. [Seedream 4.0-5.0 提示词指南](https://docs.volcengine.com/docs/82379/1829186?lang=zh)
3. [Seedream 助力 Seedance 生视频最佳实践](https://docs.volcengine.com/docs/82379/1951250?lang=zh)

### 视频与短剧

1. [Doubao Seedance 2.5 教程](https://docs.volcengine.com/docs/82379/2607688?lang=zh)
2. [Doubao Seedance 2.0 提示词指南](https://docs.volcengine.com/docs/82379/2222480?lang=zh)
3. [Doubao Seedance 2.0 系列教程](https://docs.volcengine.com/docs/82379/2291680?lang=zh)
4. [视频生成 API](https://docs.volcengine.com/docs/82379/1520757?lang=zh)
5. [火山剧创短剧创作](https://docs.volcengine.com/docs/87744/2261814?lang=zh)
6. [火山剧创标准剧本格式](https://docs.volcengine.com/docs/87744/2277069?lang=zh)
7. [火山剧创新功能发布记录](https://docs.volcengine.com/docs/87744/2261810?lang=zh)
8. [火山剧创资产管理](https://docs.volcengine.com/docs/87744/2261816?lang=zh)

### 社区参考实现

1. [Seedance 2.0 Skill OS](https://github.com/Emily2040/seedance-2.0)：用于数据契约、Prompt 编译、连续性、Take Review 和测试方法参考；不是平台能力的官方依据。
2. [OpenToonz](https://github.com/opentoonz/opentoonz)：用于 Xsheet/Timeline、分层、描线上色和二维摄影生产结构参考。
3. [LayerInbetween](https://github.com/MarkMoHR/LayerInbetween)：用于原画之间的线稿补间、遮挡处理和自动分层实验。
4. [AniDoc](https://github.com/ant-research/AniDoc)：用于角色设计参考约束下的连续线稿上色实验。
5. [BasicPBC](https://github.com/ykdai/BasicPBC)：用于赛璐璐线稿区域匹配与批量上色研究；代码仅限非商业用途。
6. [ToonCrafter](https://github.com/Doubiiu/ToonCrafter)：用于关键画之间的短序列和草图约束实验。
7. [Index-AniSora](https://github.com/bilibili/Index-anisora)：用于动漫视频生成、关键帧控制和动漫专项评价研究。
8. [AgentCine](https://github.com/pengchengneo/AgentCine)、[PenShot](https://github.com/neopen/story-shot-agent)、[OpenMontage](https://github.com/calesthio/OpenMontage)：用于 Agent 图执行、审批、异步任务、重试、记忆和追溯设计参考。

## 18. GitHub 开源项目的采用结论

截至 2026-08-20，尚未发现一个成熟仓库能够独立完成“长篇小说 → 忠实改编 → 高质量日式手绘二维动画成片”的全链路。项目采用“Agent 工作流参考 + 传统二维生产数据契约 + 分工序专项模型 Provider”的组合方式，不 Fork 某个一键视频项目作为系统底座。

| 项目 | 类型 | 本项目中的位置 | 许可或成熟度边界 |
|---|---|---|---|
| Seedance 2.0 Skill OS | Agent 规则与生产知识 | Director Planner、Prompt Compiler、连续性、重拍和质检知识库 | MIT；不代替二维生产工序 |
| OpenToonz | 完整二维动画制作软件 | Xsheet、Timing、分层、描线上色、摄影和工作台数据模型参考 | Modified BSD；首版不要求直接嵌入桌面软件 |
| LayerInbetween | 二维线稿自动补间模型/工具 | Inbetween Provider | GPL-3.0、项目较新；先做隔离实验和许可评估 |
| AniDoc | 连续线稿参考上色模型/工具 | Cleanup/Color Provider | Apache-2.0；只负责上色一致性，不产生合格原画 |
| BasicPBC | 赛璐璐线稿批量上色模型/工具 | Color Provider 研究候选 | S-Lab License 1.0，仅非商业使用；不能作为商业默认实现 |
| ToonCrafter | 卡通关键画短序列生成模型/工具 | 特定镜头补间候选 | Apache-2.0；不能代替 Animation Director 和 Timing Sheet |
| Index-AniSora | 动漫视频模型、训练/推理代码和评测体系 | Shot Generation / Evaluation Provider | 仓库 Apache-2.0，但具体权重及附加条款必须逐版本审计 |
| AgentCine / PenShot / OpenMontage | Agent 平台和编排参考 | Workflow、审批、任务、记忆、路由和追溯参考 | 不具备完整 Layout→原画→中间画→上色→二维摄影生产链 |

### 18.1 AniSora V1.0 的准确定位

AniSora V1.0 是基于 CogVideoX-5B 的动漫视频生成模型及其训练/推理实现，不是 Agent，也不是完整生产线。它可以接收 Prompt、参考图、首尾帧、关键帧或多帧控制并生成短镜头；它不会阅读整本小说、制定改编方案、完成导演解读、建立 Art Bible、制作 Layout 或决定关键姿势。

在本项目中的调用关系固定为：

```text
Storyboard + ArtBible + Layout + TimingSheet + KeyAnimationPlan
  -> Prompt Compiler / Provider Adapter
  -> AniSora V1.0
  -> Candidate Clip
  -> Quality Evaluator / Take Review
```

AniSora V1.0 只能是一台可替换的动漫镜头生成机器。Agent 和确定性 Workflow 才负责导演、生产管理、上下文、门禁、返修和连续性。

明确不采用动态漫画、Ken Burns 推拉、纸片骨骼或“每镜一张图直接图生视频”作为主生产路线；相关项目最多借鉴任务管理、素材版本和时间线 UI。

## 19. 文档维护与变更记录

1. 本文件是唯一设计入口；新增结论先合并到对应章节，不再维护平行版总设计。
2. 新增或修改节点必须记录输入、必需/可选/禁止上下文、模型或工具、输出、门禁和失败去向。
3. 调整数据传递时同步更新 Context Policy 和 Schema。
4. 节点只提交提案；Story Bible、正式资产和 Accepted Take 的晋级由 Workflow 与门禁决定。
5. 不再用 AI 生成图片表达精确架构；以本文件的结构化文字为准。

| 日期 | 变更 |
|---|---|
| 2026-08-20 | 将独立《设计.md》内容整理合并进核心总文档；系统起点改为一本小说，目标媒介锁定为手绘二维动漫。 |
| 2026-08-20 | 建立 29 节点小说改编与二维动画生产链，Agent 从 5 类扩展为 8 类，并补齐原著保真、作画分级和按工序返修。 |
| 2026-08-20 | 调研二维动画相关 GitHub 项目，确定“工作流 + 二维数据契约 + 专项 Provider”组合路线，并明确 AniSora V1.0 是模型而非生产线。 |

在线官方文档优先于项目内的本地资料副本。官方参数、价格或模型能力变化时，先更新本总文档，再调整代码和回归基线。
